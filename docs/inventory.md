# Inventory — blocker & decision log

**Convention:** blockers get logged here and work continues. The run never pauses to
ask. When something blocks, write the entry, take the documented fallback, keep
going. Unresolved entries are triaged in Phase 6, not mid-flight.

**Anti-race — NEVER `git stash` on the shared tree.** Several roles hold uncommitted work
here at once. `git stash` is tree-wide: it snatches *other* roles' in-flight edits, and a
`pop` that conflicts leaves someone else's work in a state they did not create and cannot
see coming. The rule: **commit your own pathspec first, then sync.** `git commit -- <your
paths>` then `git pull --ff-only` touches nothing but yours. If you must sync before you
can commit, the answer is to commit a smaller pathspec, not to stash. (Learned during D21:
syncing inventory.md at HEAD required stashing the scout's mid-flight `game/` tree. It
round-tripped cleanly and nothing was lost — that was luck, not design.)

Statuses: `open` · `fallback-taken` · `resolved` · `accepted` (we're living with it).

**ID allocation — pull and re-check HEAD immediately before push; on collision the later writer moves.**
Claim the next free number from HEAD, not from a ledger or a stale read. A number is only real once it
is committed in this file, and concurrent writers *will* race the same number otherwise (B23–B26 cost
~3 turns; B41 raced again). If the later writer's ID is already cited elsewhere in the repo, move
whichever side has fewer references instead — the goal is a clean map, not a moral one.

**Standing convention — every boot/CI check must be negative-controlled.** Deliberately break the
condition and confirm the check *fails* before trusting that it passes. A check that has never been
seen to fail is not evidence; it is decoration. Earned twice: **B18** (`SIM_CHECK` validated the tick
rate against its own constant, so it returned PASS at 20 Hz while the scaffold contradicted the spec
— it could not have caught the bug it appeared to cover) and again in **B22**'s `filter_check`. This
is the concrete form of **B34**'s meta-finding: reasoned-but-unmeasured claims are the recurring bug
class here, and a green check nobody has falsified is exactly that class wearing a test's clothing.

## How to add an entry

Append to the table. Keep the fallback column filled in — an entry without a
fallback is an unanswered question, which is the one thing that can stall a run.

## Blockers

| # | Date | Phase | Blocker | Fallback taken | Status |
|---|---|---|---|---|---|
| B1 | 2026-07-17 | 1 | `engine-choice.md` does not address the fixed-timestep determinism bar — the word never appears. Godot's `_physics_process` fixed rate, float determinism, and node iteration order are all unexamined. Recommendation rests on install friction, iso tilemap support, and HTTP ergonomics only. | Not treated as reversing: the sim core is being written as engine-independent plain data + our own tick, so Godot owns the frame loop but not the sim loop. The scaffold in flight is the empirical test. Carried into Phase 4's replay check. | fallback-taken |
| B2 | 2026-07-17 | 1 | `engine-choice.md` assumes a "local HTTP API backend" framing, but `forge-backend-survey.md` has not landed — the fit-with-backend bar is graded against an assumption, not a finding. | **Resolved by the survey.** forge-api is REST-over-HTTP (ASP.NET Core/Kestrel) with SignalR hubs, so an HTTP seam is real and Godot's `HTTPRequest` advantage holds — but only on the cold path (load/seed reads). The hot path is not HTTP at all (see D6). | resolved |
| B3 | 2026-07-17 | 1 | **Cross-check finding, not in either report.** The survey's hot path persists sim checkpoints *directly to Postgres, bypassing EF Core*. Godot/GDScript has no built-in Postgres client — only `HTTPRequest`. As written, the two reports' recommendations don't compose: the engine can't reach the datastore the survey specifies without a GDExtension or a third-party addon (new plugin risk, exactly what D4 chose Godot to avoid). | Invert the ownership: the **adapter service owns all Postgres access**, Godot talks HTTP/WebSocket to the adapter only. Checkpoints go out as HTTP posts on the cold path, not DB writes from the engine. Costs nothing — checkpoints are already off the tick path — and keeps Godot dependency-free. | fallback-taken |
| B9 | 2026-07-17 | 1 | **RESOLVED 2026-07-17 — was slice-blocking. `factory-math-v0.md` misses the "v0 recipe set" bar.** It specifies the recipe *formalism* (§2.2 bipartite graph, the `Recipe` struct, §2.3 stoichiometry matrix) and §2.4 load-time validation rules — but ships no actual content. The only concrete recipes are three rows of a `[CAL]` worked example (2 plate→1 gear, 1 ore→1 plate, ore extraction). §2.4 validates content that does not exist. The vertical slice needs at least one real source→belt→machine→output recipe chain to run at all. | Promote the §3.2 worked example to *be* the v0 set — ore → plate → gear is already dimensioned (`d`, `σ`, rates, machine counts) and is exactly the slice's shape. Cross-check against D6: the survey maps `Part`→item and `BomRevision`→recipe, so the v0 set can seed from real forge MRP data on the cold path rather than being invented. Land the table in the spec during Phase 4. **Delivered as predicted:** `data/recipes-v0.toml` (canonical) + `docs/recipes-v0.md` (rationale + machine-run §2.4 proof). The ore→plate→gear worked example was promoted to real content exactly as the fallback specified. Rules 1,2,3,5,6 pass programmatically; rule 4 vacuously — split out as B19. Graded: passes. | resolved |
| B10 | 2026-07-17 | 1 | `factory-math-v0.md` contradicts itself on Fx32 rounding mode. §1.2 states the rule as **truncate toward zero** and says "pick one and pin it in a test" — but §8 Q2 re-opens it as undecided pending a numeric error budget over a 10⁷-tick run. Determinism is undefined until one wins, and D5/Phase 4's replay check depends on it. | Pin **truncate-toward-zero** now, per §1.2, and put it in a test as the spec instructs. Truncation bias is a *calibration* concern, not a correctness one — a biased-but-deterministic sim replays fine. Keep §8 Q2's error budget as post-slice work that may retune, not re-litigate. Not slice-blocking once pinned. | fallback-taken |
| B8 | 2026-07-17 | 1 | `isometric-design.md` soft-misses the **sprite budget** bar. The only figure is "~20k sprites/frame" in §7, posed as a *question to the engine* rather than a budget the doc commits to — and §8 ("What Phase 0 commits to") omits a budget entirely. The §5 art contract and §6.4 LOD tiers imply density limits without ever stating the number the renderer must hold. | Not blocking the slice — the vertical slice is one belt and one machine, nowhere near any budget. Derive the real number from the doc's own figures during Phase 5 (~30×34 tiles visible at 1.0 zoom ≈ 1k tiles, LOD tiers cap the rest) and land it in §8. The 20k figure becomes a scaffold measurement under Q6 rather than an assumption. | fallback-taken |
| B6 | 2026-07-17 | 1 | **Question-numbering collision between orchestrator and this log.** The orchestrator refers to "Q5 = isometric-design §7 engine disqualifiers (routed to scout)" and "Q4 = empirical stack bring-up (forge expert)". This log already had Q4 = kiosk-auth-standalone and Q5 = schema-source-of-truth, assigned earlier. Two different questions share each number. | Kept this log's numbering as authoritative (it was assigned first and is referenced by D6/D7/B5). Filed the orchestrator's two items as **Q6** and **Q7** rather than overwriting. Anyone citing "Q4"/"Q5" from an orchestrator message means Q7/Q6 here. **Recurred on the decision series:** the orchestrator asked for the LP-planner call to be logged as "D6", but D6 here is the adapter/BFF decision — filed as **D9**. Mapping so far: orchestrator Q4→Q7, Q5→Q6, D6→D9. If this keeps happening, the fix is the orchestrator naming items rather than numbering them. **CONVENTION ADOPTED 2026-07-17 (orchestrator):** the **proposer takes the next free number** straight from inventory.md; the orchestrator's map follows the log, never the reverse. inventory.md is authoritative for IDs. **Collision cleanup 2026-07-17 (SUPERSEDED, then re-pinned):** four real duplicates existed (B23–B26 each held two different items — PM entries vs forge/iso-expert entries written concurrently). The PM first moved its own entries to B27–B30; the orchestrator then **pinned the opposite map as authoritative** — the PM's inventory.md assignments win and the ledger-only claims renumber. **Final pinned map applied 2026-07-17:** B23 dotnet-hosting-check (scout) · B24 golden-excludes-transport · B25 staged-bench-artifacts · B26 D14-sweepup · B27 write-path-live (ex-"B24") · B28 WorkCenter-mapping-skipped (ex-"B25") · B29 forge-seed-zero-BOM (ex-"B26") · B30 godot-json-floats · B31 godot-tilemap-staggered-basis (iso expert logged it as "B23"; relabelled). Verified zero duplicate IDs. **Cost of this episode: ~3 turns.** The convention above exists so it doesn't recur — the log is the registry, and a number is only real once it is *in* the log. | resolved |
| B7 | 2026-07-17 | 1 | `factory-math-v0.md` specifies `Fx32` fixed-point, "no float anywhere in simulation code," and refers to "the sim **crate**" — vocabulary and numeric model written for Rust/Bevy, not the locked Godot 4.7 (D4). GDScript's floats are f64 and it has no native fixed-point; Fx32 is implementable on its 64-bit ints but is exactly the hot-path arithmetic where GDScript is weakest. | Not reversing D4 and not doc work. The substance (integer ticks, deterministic order, no wall-clock) is engine-agnostic and *strengthens* D5 — a sim written this way re-hosts to Bevy cheaply if D4's perf trigger ever fires. The fixed-point hot path is the leading candidate for the C#/GDExtension escape hatch D4 already reserves. Route to the scaffold as a measurement, not a debate. **2026-07-17: MEASURED.** Same-shape Q16.16 add/mul micro-benchmark (4M ops, identical accumulator logic, checksums match): GDScript (`game/scripts/bench_fixed_point.gd`, run via `--headless`) = **15.2M ops/sec**. C# (`bench/csharp`, plain `dotnet run -c Release` console app — not Godot's Mono/.NET build, which wasn't installed; a fair proxy for C#-side arithmetic throughput since this is CPU-bound scalar integer math with no engine API calls) = **280M ops/sec**, ~**18x** faster. Reading: GDScript is very likely fine for tick orchestration and non-hot-path logic (15M ops/sec is a lot of headroom at 20 tps), but a sim core doing Q16.16 arithmetic over thousands of belts/machines per tick should default to the **C#/GDExtension escape hatch** for the actual fixed-point hot path per D4, not pure GDScript. Not yet a measured wall for GDScript specifically (no in-sim-shape workload run) — this is the rough factor the sizing call needed, not a final verdict. | resolved |
| R1 | 2026-07-17 | 2 | **(= review A1)** `TileMapLayer` must carry terrain only — not machines, belts-with-items, or items. A tilemap Y-sorts per-cell and has no concept of a multi-tile entity's far corner, so a 3×3 assembler sorts as a 1×1 at its origin — the exact classic bug `isometric-design.md` §3 calls out. Includes a rename to mark the boundary. | Fix dispatched to the iso expert this window. Numbering is bookkeeping only — nothing blocked on it. | fix-in-flight |
| R2 | 2026-07-17 | 2 | **(= review A2)** No `Camera2D` exists, and `TickLabel` is a child of `Main` (a `Node2D`) — i.e. it lives in **world space**, so it will pan with the factory the moment a camera is added. §6.3 requires overlays screen-space, fixed-size, never occluded. Fix is `Camera2D` + `CanvasLayer`. | Fix dispatched to the iso expert this window. | fix-in-flight |
| R3 | 2026-07-17 | 2 | **(= review B1 — *review-local* B1, not this log's B1)** The placeholder tile is an opaque rectangle, so the isometric grid is invisible. Fix is a diamond-alpha placeholder tile. | Fix dispatched to the iso expert this window. | fix-in-flight |
| R4 | 2026-07-17 | 2 | **(= review B2 — *review-local*)** Default texture filter is linear → diagonal shimmer, which `isometric-design.md` §1 names as the specific reason 2:1 dimetric was chosen (pixel-exact 2px/1px diagonals). Linear filtering discards that benefit. Fix is `Nearest`. | Fix dispatched to the iso expert this window. | fix-in-flight |
| B24 | 2026-07-17 | 4 | **`golden-excludes-transport`** (number assigned per orchestrator; proposed and correctly self-flagged by the mathematician in `golden-v0.md` §3). The golden vector covers the **ore→plate→gear craft chain only**. Belts, inserters, power, and fluids are **not exercised** — so a green golden proves the §3.1 machine-rate/remainder-carry path and nothing else. The risk is misreading it as full coverage: it is D15's acceptance test, and passing it is *necessary but not sufficient* for the vertical slice, which is defined as source→**belt**→machine→output. The belt is exactly what the golden does not test. | Off-slice as a *gap in the test*, not in the code — the slice still needs working belts and the §3.3 run-compression math regardless. Fallback: treat the golden as the craft-path gate only, and hold `recipes-v0.md` §3's separate acceptance test (headless 10k-tick run, measured gear output 5.0/s ± stated slack, per-stage `u` matching the table) as the transport-inclusive gate. That second test already exists on paper and covers belt cap (10 ore/s vs one lane at 7.5/s) — it should run before Phase 6 signs the slice off. **UPDATE 2026-07-17 — belts discharged, NOT the whole item.** `transport-v0.md` (`8aaa983`) specs the deterministic transport model, and `refsim_v0.py` + the new `transport` golden scenario (`b3b04b6`) gate it: ore rides a 20-tile tier-I lane, gears settle at **exactly 3.75/s** (lane caps at Θ_lane = 7.5 ore/s, under the miners' 10.449768/s → the belt is the bottleneck), the saturated lane holds **exactly 80 items in 1 run** (spacing invariant + the O(runs) compression claim), and tick 600 shows **zero gears with 62 items in flight** (640-tick transit), pinning latency and throughput as independent properties. All 8 published steady/backpressure hashes are byte-identical — verified by regeneration, not assumed. **THREE RESIDUALS KEEP B24 OPEN:** **(1) Inserters** — §3.3's `Θ_ins` (swing time, stack bonus, position-dependence) is specified but unimplemented and ungoldened; machines still move items directly, so *the usual real bottleneck* is absent. This is now the largest hole and the one most likely to be misread as covered. **(2) Splitters** — `transport-v0.md` §6 pins the alternation rule and requires `in_next`/`out_next` to be hashed, but no golden scenario has a splitter, so that rule and its hash contribution are **untested** (same shape as B19's `cycle-validator-untested`). **(3) C# parity** — belts exist in `refsim` only; `Forge.Sim` has none, so the `transport` vector has no implementation to gate yet. Deferred to a follow-up dispatch by design; until it lands, D21's `beltDeltas: null` ("belts not modelled") stays correct, and it should stop being null the moment C# belts land — the null-vs-`[]` distinction becomes load-bearing then (see B45). Phase 6 sign-off should turn on (1) at minimum. | open |
| B25 | 2026-07-17 | 2 | **Hygiene: `bench/csharp/bin` + `obj` build artifacts in the shared index.** Owner: scout. | **ALREADY FIXED — verified 2026-07-17, no action needed.** `.gitignore` exists at the repo root and carries `bench/csharp/bin/` + `bench/csharp/obj/` (alongside `game/.godot/`, `tools/godot4`, `tools/*.zip`, `__pycache__/`). `git ls-files` shows **0** tracked artifacts under either path and `git status` shows none staged. The dispatched fix landed before this entry was written. No sweep-commit risk remains. **Generalized 2026-07-17:** with `sim/Forge.Sim` + `sim/Forge.Sim.Tests` now landing (D15) and `tools/dotnet_hosting_check`/`tools/godot4_mono` added for B23, tightened the patterns to `**/bin/`, `**/obj/`, `**/.godot/` (recursive, not per-path) so every current and future C#/Godot subproject is covered by one rule instead of enumerating paths one at a time. | resolved |
| B26 | 2026-07-17 | 4 | **D14 was decided but not enforced — the dropped build step exists on disk and is unreferenced.** `tools/recipes_to_json.py` and its output `data/recipes-v0.json` are present and untracked. The script's own docstring reads *"D13 build step: transform the authored TOML content into JSON the sim core can load… the engine is Godot (D4), which ships no TOML parser."* That is D13's rationale, which **D14 superseded** — the C# sim core now parses `recipes-v0.toml` directly via NuGet, so this transform has no consumer. Confirmed: nothing outside the script and its own output references `recipes-v0.json`. This is the mathematician's pre-correction work, exactly as the orchestrator described; it just wasn't swept up. | **Delete `tools/recipes_to_json.py` and `data/recipes-v0.json` before anyone sweep-commits**, or the repo ships a second content artifact that looks authoritative and will drift from the TOML the moment content changes — the precise failure D14 chose zero build steps to avoid. Both are untracked, so this is a delete, not a revert. If they land in a commit first, this becomes a real cleanup instead of a free one. Assign to the mathematician alongside D15. **DISPOSITION CORRECTED 2026-07-17 — NOT a free delete; governed by D17.** The PM's original "delete both" recommendation is **superseded**: it arrived after the mathematician had already committed both files (`a2f76bd`), and it undervalued the script. Per **D17**: `tools/recipes_to_json.py` is **KEPT** and repurposed as the §2.4 content validator (`--check` mode for CI) — **do not sweep-delete the .py**; only the *generated* `data/recipes-v0.json` is deleted, since the generated artifact was the actual drift hazard, not the validation logic. Cleanup is in flight with the mathematician; **B30 closes when that commit lands.** (Numbering note: the orchestrator's ledger called this item "B30" — in this log B30 is `godot-json-floats`; this item is **B26**, per the B6 convention that the log is the registry.) **SCOPE GUARD (added 2026-07-17 at iso's request):** this entry covers **exactly two files** — `tools/recipes_to_json.py` (KEPT per D17) and `data/recipes-v0.json` (deleted). It is **not** a general licence to delete unreferenced code. Named explicitly as OUT of scope: **`Iso.screen_to_world` and `Iso.depth_key`** — these are *intentionally dormant*, not dead. `isometric-design.md` §1/§2 require the inverse transform and far-corner depth key to exist and carry `z` from the start precisely because retrofitting them later is a rewrite (the same reasoning as R5); iso is wiring them into placement/hover and the depth draw list now (post-`c9fc950`). **Unreferenced ≠ dead** — a symbol with a spec mandating it and an owner actively wiring it is neither. | resolved-pending-commit |
| B23 | 2026-07-17 | 2 | **`dotnet-hosting-check`** — number reserved ahead of the scout logging it (not yet in the file as of this entry; scout to fill in the detail). **Flagging the sequencing risk now, because two decisions already depend on the answer:** D14 puts the content loader in C# and D15 dispatches an 80-min C# sim core — but *nothing has yet verified that Godot 4.7 can host C# at all* on this box. B7's benchmark measured C# via a plain `dotnet run -c Release` console app and explicitly noted **Godot's Mono/.NET build "wasn't installed"**. `tools/fetch_godot.sh` (B14) fetches the standard build; the .NET-enabled variant is a *separate* download, and the .NET SDK is a separate dependency again. So the C# figure that justified D14/D15 was never measured inside the engine that has to run it. | **RESOLVED — PASS, verified 2026-07-17.** (1) dotnet SDK: **10.0.110** (`/usr/lib/dotnet`, `dotnet --list-sdks`), runtime `Microsoft.NETCore.App 10.0.10` — this is what B7's C# benchmark used (`cd bench/csharp && dotnet run -c Release`), confirmed root-free (no sudo anywhere in the invocation). (2) Fetched the .NET-enabled Godot build root-free via new `tools/fetch_godot_mono.sh` (sibling to `fetch_godot.sh`, same GitHub-release pattern): `Godot_v4.7-stable_mono_linux_x86_64.zip` → `tools/godot4_mono/` → `godot4 --version` reports `4.7.stable.mono.official.5b4e0cb0f`. (3) Built + ran a trivial C# script inside it: `tools/dotnet_hosting_check/` (minimal `.csproj` targeting `net8.0` via `Godot.NET.Sdk/4.7.0`, one `Node`-derived class). First-run needs `--headless --editor --build-solutions --quit` once to generate/build the C# glue (this is a one-time project-init step, not per-run); afterward a normal `--headless` run executes `_Ready()`: `DOTNET_HOSTING_CHECK result=PASS`. Notably this worked with **only the .NET 10 SDK/runtime installed, no net8.0 runtime pack** — Godot's mono build resolves this on its own (bundled hostfxr/runtime resolution), so no additional SDK install was needed. **D14/D15's C# sim core can be hosted in-engine on this box.** No fallback needed (GDExtension / separate-process were the fallbacks in play; neither is required). | resolved |
| R5 | 2026-07-17 | 2 | **(= review B3 — *review-local*) ASSIGNED — in the iso expert's in-flight R1–R4 scope (orchestrator correction 2026-07-17; my earlier "unassigned" read was wrong).** Was omitted from the dispatched R1–R4 list; now added, so it slips only by decision. The review lists **five** actionable findings (A1, A2, B1, B2, B3); the orchestrator's R1–R4 covers four and omits this one: **no `z` in the coordinate path** — the scaffold's cells are `Vector2i` and Godot's `map_to_local` is 2D. `isometric-design.md` §1 commits to carrying `z` through all transforms (`TILE_Z=16`) *even though v1 is single-level*, on the explicit grounds that **retrofitting elevation into a 2D-only transform is a rewrite** while carrying an unused `z` costs one add. | **Add to the iso expert's in-flight scope** — it is the cheapest of the five now and the most expensive later, which is precisely the trade §1 pre-decided. Not slice-blocking (v1 is single-level), so if the window is tight it can slip — but it should slip as a *decision*, not by being dropped off a list. Also note the review's internal A/B numbering collides with this log's B-series (its B1/B2/B3 ≠ our B1/B2/B3); R-prefix keeps them apart — see B6. | assigned - in flight |
| B19 | 2026-07-17 | 1 | **`cycle-validator-untested`** (number assigned per orchestrator request; proposed and correctly self-flagged by the mathematician in `recipes-v0.md` §4). Spec §2.4 rule 4 — every cycle passes the spectral-radius test — passes only **vacuously** against the v0 set: the ore→plate→gear graph is a DAG, so there are no cycles and the validator never executes. The check that stops a recipe loop from manufacturing matter from nothing has never run. | Off-slice, no fallback needed — the slice ships DAG content, so the untested path is also unreachable in it. Close with a unit test over a synthetic cyclic fixture (cheap, ~1 fixture + 1 assert) rather than waiting for v1 loop content. Note the tie to B9's §5: real forge MRP BOMs **do** contain cycles (rework/regrind loops), so a D6 cold-path importer would exercise rule 4 immediately — this must be closed *before* MRP seeding, not after. **SANITY-CHECK vs the live seeding task (2026-07-17): NON-CONFLICTING — B19 does not block Parts+BOM seeding.** B19's constraint reads "close before MRP seeding", meaning *importing forge MRP data INTO the game* — real BOMs contain rework/regrind loops, so an importer would feed cycles to an untested validator. The forge expert's task runs the **opposite direction**: pushing `recipes-v0` (a DAG, cycle-free by construction, already validated) OUT into live forge to fix B26's empty-BOM seed. Nothing cyclic reaches the validator, so the ordering constraint is not engaged. B19 stays off-slice and open; it re-engages only if a cold-path importer pulls real forge BOMs back into the game. **B29 CROSS-CHECK 2026-07-17: NON-CONFLICTING — B19 does not block the in-flight forge seeding.** Same reasoning as the earlier MRP check, and it holds for the same reason: **direction**. B29 pushes `recipes-v0` (a DAG, cycle-free by construction, already validated against §2.4 rules 1/2/3/5/6) **OUT** into live forge to fix its zero-BOM seed. B19 guards the **INBOUND** path — pulling real forge BOMs (which carry rework/regrind loops) back into the game, where they would hit a spectral-radius validator that has never executed. Outbound DAG content cannot exercise the untested cycle path, so the constraint is not engaged. B19 stays off-slice and open; it re-engages only on a cold-path importer. **Note D17 helps here:** the surviving `recipes_to_json.py` validator is the natural home for B19's synthetic cyclic fixture. | open |
| B20 | 2026-07-17 | 2 | **Content format assumes Rust, not the locked engine — same root as B7.** `recipes-v0.md` states the set is "parsed by the sim core with `serde` + `toml`". The sim core lives in Godot (D5; `sim_clock.gd` is the scaffold's tick owner), and **Godot has no TOML parser** — it ships JSON and `ConfigFile` (INI-like, not TOML). `serde` is a Rust crate and is unreachable from GDScript. Confirmed: no parser of any kind is wired in `game/` yet, so nothing currently reads the canonical data file. | Keep TOML as the authored source of truth — it earned its place (comments carry the derivations, which is the whole reason it beat JSON). Add a build-step transform to JSON, which Godot parses natively, and load that. The alternative — retyping content into `ConfigFile` — would split the source of truth, which is worse than a build step. Not slice-blocking yet only because no loader exists; **it blocks the moment Phase 4 wires content in.** **RESOLVED — ratified as D13. OWNER: factory-mechanics-mathematician** (queued behind their in-flight golden-vector work; **DISCHARGED via D14 — no build step exists. Trail: B20 → D13 (build-step JSON) → **superseded by D14** (C# sim core parses TOML directly via NuGet, zero build steps). `recipes-v0.toml` remains the single authored source. Loader question CLOSED. Caveat: the pre-correction transform still sits on disk untracked — see B26**). **Hard-block trigger stands: Phase 4 content wiring.** (TOML authored, build-step JSON transform, owner mathematician, trigger Phase 4 content wiring; moot if B7 fails and D4 fires). | resolved |
| B16 | 2026-07-17 | 2 | **SLICE-BLOCKING. Tick-rate contradiction between the scaffold and the math spec.** `game/autoload/sim_clock.gd:5` sets `TICK_RATE = 20.0`. `factory-math-v0.md` §1.1 pins `TICK_HZ = 60` ("High — genre standard", Appendix A), Q3 was closed on 60, and `data/recipes-v0.toml:13` sets `tick_hz = 60` with every rate derived from it (`R = 60·σ/d`; durations 60/192/30 ticks). At 20 Hz the whole v0 recipe set runs at **⅓ its designed rate** — the ore→plate→gear chain silently produces 1.67 gear/s where the spec says 5. | **Fixed.** `sim_clock.gd:5` → `TICK_RATE = 60.0`. Headless self-check re-verified: 3.002s real time → 181 ticks vs 180.1 expected, well within tolerance, still PASS. | resolved |
| B21 | 2026-07-17 | 1 | **Ground-truth bring-up (Q7/Q4 orchestrator) initially failed.** Running the documented command verbatim — `docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d` from `forge-deploy/` — recreated `forge-api` crash-looping (exit 155, "No .NET SDKs found... global.json requests 10.0.100"). Root cause: `forge-deploy/forge-api/` is a stray root-owned directory (docs/storage/tools only, no source) instead of resolving to the real sibling repo, so the dev overlay's `./forge-api:/app` bind mount shadowed the real app with that near-empty stub. `docker-compose.dev.yml`'s `./forge-api` context/volume is only valid from a cwd where `forge-api` resolves to the real repo (e.g. via `--project-directory`); it is **not** consistent with the base `docker-compose.yml`'s `../forge-api`, and running literally "in forge-deploy/" as instructed hits the inconsistency. | Dropped the dev overlay and rebuilt/ran forge-api from the **base** `docker-compose.yml` only (`docker compose -f docker-compose.yml up -d --build forge-api`, from `forge-deploy/` — its `../forge-api` context is correct and it has no `/app` bind mount). Came up healthy. Re-verified full ground truth after recovery: kiosk login (`POST /api/v1/auth/kiosk-login`, throwaway barcode+PIN test user, PBKDF2-hashed to match `SetPinHandler`, deleted after) → JWT → `GET /api/v1/parts` → **200 with real seeded data**. Bring-up: **pass**, via the base-compose fallback, not the dev overlay as literally instructed. | fallback-taken |
| B17 | 2026-07-17 | 2 | **`sim_clock.gd` has no catch-up cap — unbounded `while` in `_process`.** `factory-math-v0.md` §1.4 requires `MAX_CATCHUP_TICKS = 8` and the rule "if the sim cannot hit rate, **the game slows down** — it does not drop ticks and it does not scale dt." As written, one long frame (a hitch, a breakpoint, a laptop suspend) leaves a large `_accumulator`, and the loop runs every owed tick in one frame — which lengthens the frame, which owes more ticks. Classic spiral of death. | **Fixed.** `_process` now bounds the drain loop with `ticks_this_frame < MAX_CATCHUP_TICKS` (=8); when the cap is hit, `_accumulator` is left holding whatever backlog remains — never zeroed — so it drains across subsequent frames instead of dropping ticks or rescaling dt, per §1.4/§3.1. | resolved |
| B18 | 2026-07-17 | 2 | **`SIM_CHECK PASS` is self-referential and cannot fail on B16.** `main.gd:29` computes `expected := SimClock.TICK_RATE * elapsed` — it validates the clock against *its own constant*, so it returns PASS at 20 Hz, 60 Hz, or any other value. It genuinely proves the accumulator tracks wall-clock and that we own the loop (Q2), but it is structurally blind to whether the rate is the *agreed* rate. It passed while the scaffold contradicted the spec, which is exactly the bug it reads as covering. | **Fixed.** `sim_clock.gd` now has `_validate_tick_rate_against_recipes()`, called from `_ready()`: a purpose-built single-key line scan of `data/recipes-v0.toml` (not a general TOML parser — that's D13's still-pending build step, owned by the mathematician; a full parser here would be exactly the plugin-risk D4 avoids) that reads `[meta] tick_hz` and `push_error`s on any mismatch with `TICK_RATE`. Verified silent (no error) at the now-matching 60/60. | resolved |
| B12 | 2026-07-17 | 3 | **`adapter-contract-v0.md` silently drops D6's "sim owns its own PG tables."** D6 (ratified) = separate service, own tables, EF Core bypassed on the hot path. The contract routes *everything* through forge-api HTTP — including checkpoint-time inventory writes (§5 `receive-stock`/`use-stock`) and production-run progress (§6). There is no own-tables story anywhere in it, and §7 leaves replay/seed-log persistence unaddressed while asserting "forge has no concept of this." | **Accept the divergence for the slice; it is arguably better.** The ActivityLog tax D6 was built to dodge is only disqualifying *per tick* — at batched checkpoint cadence (~1/s) it is exactly the human-rate ERP traffic forge-api is designed for, and the contract's explicit nothing-per-tick rule preserves D6's actual intent. Bonus: no factory tables means Q5 (schema source of truth) never has to be answered this run. **But** the replay log (D5/Phase 4's exit condition) still needs somewhere to live — flat file sim-side is fine for the slice. Revisit own-tables only if checkpoint volume grows. **RATIFIED 2026-07-17 by the orchestrator:** all persistence goes via forge-api HTTP; sim-owned PG tables dropped for the slice; Q5 moot; replay log is a flat file sim-side. Superseding decision recorded as D10 — the contract is now correct as written and needs no revision. | resolved |
| B13 | 2026-07-17 | 3 | **Capability gap the contract doesn't close, likely to bite the skeleton now in flight.** Q4 established kiosk auth is an *alternate credential only* — same RBAC/capability gates. The live verification used an `Engineer` test user against `GET /api/v1/parts` (a read). But §6's start-run needs `CAP-MFG-COMPLETE`, §5's `use-stock` enforces the reserved-stock invariant (S-RI1), and §5's `adjust` is Admin/Manager only. **Nothing verified that the kiosk-credentialed game operator actually holds the capabilities the write path requires** — the proven round trip was read-only. | Not slice-blocking and not a re-grade: verify at skeleton time, not by doc work. If `Engineer` lacks `CAP-MFG-COMPLETE`, options in preference order: grant the capability to the game-operator role (data change, no code); or drop §6's Job/ProductionRun lifecycle from the slice entirely — it is the most optional part of the contract (the slice needs items moving, not ERP order paperwork) and §5's inventory primitives alone carry the slice. Flagged to the forge expert as the first thing to hit live. | open |
| B5 | 2026-07-17 | 1 | The kiosk-auth claim (D7) is a *recommendation*, not a verified finding — the survey confirms the flow exists and is "likely the better model," but never checks whether it can be used without the ASP.NET Identity + capability stack behind it. Also note it answers player login only; the sim service's own auth is unspecified beyond "service-account/internal-network concern." | Not blocking — auth sits off the vertical slice entirely. If kiosk auth turns out to drag the full Identity stack with it, the slice runs with no auth at all and login is deferred past this run. Logged as Q4. | accepted |
| B4 | 2026-07-17 | 1 | The survey contradicts itself on adapter coupling depth. §"Answer to Q2" (lines 23–28) has the sim owning its own tables and bypassing EF Core entirely; §"Recommended integration approach" (line 78) has it "layered on top of forge's EF Core entities" as "a new minimal API project **or a dedicated controller area**". A controller area inside forge-api is materially different from a separate service — and would re-inherit the `ActivityLog`/capability tax the report itself calls disqualifying. | Resolve toward lines 23–28, the better-evidenced half: separate service, own tables, EF Core bypassed on the hot path. Recorded as D6. | fallback-taken |
| B11 | 2026-07-17 | 1 | **No self-serve API to provision a kiosk (barcode+PIN) identity.** `POST /api/v1/auth/kiosk-login` works end-to-end (empirically verified — see Q7), but creating the barcode+PIN credential requires either an already-authenticated `POST /api/v1/auth/set-pin` call (chicken/egg for a fresh automated bring-up) or a direct DB insert. Blocks a fully scripted "spin up forge + game, zero manual steps" dev/CI flow. | Verification used a throwaway test user (barcode `GAMETEST01`, role `Engineer`) inserted directly via SQL with a PBKDF2 hash matching `SetPinHandler`'s algorithm, then deleted after the check. Documented in `adapter-contract-v0.md` §7 (Gaps). Real integration work should add a thin admin-token-gated provisioning endpoint, or accept a documented DB-seed step for dev/CI. | fallback-taken |
| B14 | 2026-07-17 | 2 | `snap install godot-4` (per D4) requires passwordless sudo; this box only has interactive sudo and no TTY is available to the agent, so the snap install could not run non-interactively. **(Renumbered from a colliding B12 — the adapter agent's B12 landed on disk concurrently under the same ID; see B6 for the pattern.)** | Downloaded the official Godot 4.7-stable Linux x86_64 editor binary directly from the godotengine GitHub release (no root needed) into `game/../tools/godot4`, reproducible via `factory/tools/fetch_godot.sh`. Same exact engine version as D4 specified; no scope impact. Binary itself is gitignored (138MB). | resolved — ratified by PM 2026-07-17; scout-assigned numbering kept as-is |
| B30 | 2026-07-17 | 4 | **`godot-json-floats`** (mathematician). Godot's `JSON.parse_string` returns **all** numbers as `float` — there is no int type in its JSON parse result. Any Godot-side JSON loader must `int()`-cast under a `< 2^53` precision guard, or Q16.16 fixed-point values silently arrive as floats and the "no float in sim code" axiom (`factory-math-v0.md` §1.2) is violated at the load boundary — the one place nobody looks for it. | **Largely mooted by D14/D17 before it could bite:** the C# sim core parses TOML directly, so no Godot-side JSON loader exists on the content path, and `recipes-v0.json` is being deleted. Keep the entry live anyway — it re-engages the moment *any* Godot-side JSON consumer appears (the D6/D10 adapter HTTP seam returns JSON, and the Godot client will parse it). The guard belongs in whatever wraps that response. **RESOLVED 2026-07-17 (scout) — the re-engagement predicted above just happened.** `game/scripts/adapter_client.gd` is that wrapper: a thin `HTTPClient`-based Godot-side client for the live adapter's HTTP endpoints (`GET /health`, `POST /cold-load`, `POST /checkpoint` — not the `/hubs/sim` SignalR hub, out of scope for an HTTP wrapper). `parse_cold_load_body()` is the guarded parse point: `int()`-casts `count`/`recipesCount` (the fields the C# contract types as int — `SimContent.Items.Count`/`Recipes.Count` in `adapter/Program.cs`'s `/cold-load` handler). Negative-controlled proof in `game/scripts/adapter_client_check.gd` (scene: `game/scenes/adapter_client_check.tscn`), run **against the live adapter** (started via `ASPNETCORE_URLS=http://127.0.0.1:5299 dotnet run` from `factory/adapter/`, itself talking to the already-running forge-api+Postgres stack — real `/cold-load` response: `{"count":3,"recipesCount":2,"gaps":[...]}`, real gap text about `RAW-00002` having no BOM revision, not a stub). The check demonstrates both halves: unguarded `JSON.parse_string()` yields `TYPE_FLOAT` for `count` (bug reproduced), `parse_cold_load_body()`'s guard yields `TYPE_INT` (fixed). `ADAPTER_CLIENT_CHECK_B30 result=PASS source=live-adapter bug_reproduced=true guard_fixes_it=true`. Falls back to a canned fixture matching the live shape if the adapter isn't up (not needed this run). | resolved |
| B34 | 2026-07-17 | 2 | **Meta-finding (iso expert), low priority: reasoned-but-unmeasured engine claims are a recurring bug class.** B31 was one (the staggered-basis assumption held up under reasoning and failed against the engine). B1, B7, and B23 are the same shape — a claim about Godot's behaviour that was argued rather than run. The class is dangerous precisely because the reasoning is usually *good*; it just isn't evidence. | Policy, cheap and already implicit in how this run has gone: **on-slice claims get a boot check** (fold into the A2 camera work, where the scene-tree checks already run); **off-slice claims just get an inventory line** and wait. No new process — this is naming what Q6's `engine_checks.tscn` and B7's benchmark already did, so the habit survives after this run. | accepted |
| B31 | 2026-07-17 | 2 | **Godot's isometric TileMapLayer does not use `isometric-design.md` §2's coordinate system — and my own review (`iso-review-scaffold.md`) asserted that it did.** The review's lead "green — no action" bullet claimed `map_to_local` computes `((x-y)*w/2, (x+y)*h/2)`, identical to §2, so "we get the transform for free." **False, and asserted from memory of the docs rather than measured.** Implementing R5 added a boot check comparing the two: it failed by up to 389px. Godot uses **staggered/offset** coords at 64x32/DIAMOND_DOWN/HORIZONTAL — `cy` is a screen row and odd rows shift half a tile right, so stepping `+cy` zigzags in x (32→64→32). That is not a linear map, so it is not even a change of basis from §2's diamond axes. The 2:1 tile *shape* is as claimed; the *addressing* is not. Had the render layer been built on the claim, machines (via `Iso.world_to_screen`) would render offset from their terrain, worsening with distance from origin. | **Keep §2 canonical and confine Godot's scheme to the tilemap boundary.** §2's basis is what the §3 far-corner depth key and the engine-independent sim (D5/D8) assume, and a staggered basis makes "the far corner of a 3x3 footprint" nearly unexpressible — so §2 wins and the tilemap converts. Derived + landed as `Iso.cell_for_world` (`game/scripts/iso.gd`): `cy = x+y-1`, `cx = ((x-y) - 1 - posmod(cy,2)) / 2` (numerator always even since x-y and x+y share parity, so the division is exact; the modulo **must** be positive — GDScript's `%` truncates toward zero and returns -1 for odd negative `cy`, silently misplacing every tile above the origin). `terrain_layer.gd:_check_transform_agreement` re-proves it every boot: **`ISO_CHECK max_transform_err=0.000000 px PASS`**, headless and windowed. Contained — only terrain goes through a tilemap; layers 3-4 use `world_to_screen` directly. **Broader lesson worth keeping: this was caught only because R5 was implemented as a runtime check rather than a doc edit. The equivalent unverified claims in the review are §7 Q1/Q2/Q4/Q5 (routed to the scout as Q6) — they should be measured, not reasoned about, for exactly this reason.** | resolved |
| B41 | 2026-07-17 | 2 | **`FILTER_CHECK`'s discriminating power is an artefact of placeholder art and will decay silently as real art lands.** The B22 check counts pixels that are neither the tile fill nor the background: sound today because the atlas holds exactly **two** colours, so under Nearest (sampler returns a verbatim texel) that net catches every Linear blend. Real §5 art — three face brightnesses, per-category hue, labels — has a wide palette, and a blend of two of its texels usually lands near some third texel. The net goes slack and the check drifts toward vacuous **while still printing PASS**, which is worse than deleting it. Directly the concern raised about check-5/flat-ColorRect softness, generalised: flat placeholder art is the *reason* the instrument is sharp, not a caveat on it. | Not blocking; the slice ships placeholder art, where the check is valid and negative-controlled (Linear gives 964 blended px FAIL). Two options when real art lands, in preference order: (1) test against the **atlas palette** rather than two constants — read the generated atlas's distinct colours into a set and assert every rendered pixel is a member; still exact under Nearest, stays sharp for any art, costs one `Image` scan at boot; (2) if the palette grows too dense for (1) to mean anything, retire `FILTER_CHECK` for a **known-answer** test: render one hand-placed diamond at a known sub-pixel offset and assert exact expected pixel values. **Do not simply keep the current check and trust its PASS** — that is the B18 failure mode with extra steps. Re-evaluate at the first real-art drop. **UPDATE 2026-07-17 — this fired MUCH earlier than predicted, and confirmed the danger was understated.** It did not wait for real art: the dummy entity prisms in `entity_layer.gd` (three category hues x three face brightnesses, plus a 50%-alpha outline) broke it immediately — **642,790 'blended' px, `result=FAIL`, and the check confidently printed `default_texture_filter is not Nearest at render time`**. That diagnosis was FALSE and pointed at an innocent setting. So the decay mode is worse than 'goes quietly vacuous': it can go **loudly wrong**, accusing the wrong subsystem. Mitigated for now by scoping — `FILTER_CHECK` hides all non-terrain CanvasItems for its duration, restoring the two-colour invariant honestly, since it only ever claimed to test the terrain tile sampler. **This only defers B41**: scoping survives entities gaining art, but NOT terrain itself gaining §5 art. The palette-membership fix above is still the real answer. **RESOLVED 2026-07-17 — fix (1) implemented as prescribed.** `FILTER_CHECK`'s net is now PALETTE MEMBERSHIP against the generated atlas, read live from the new `terrain_layer.atlas_palette()` at check time; the `TILE_FILL` constant is deleted. That constant was itself a second bug hiding in the first: `render_checks.gd` held a **copy** of terrain's fill under a `# Must match terrain_layer.gd` comment — and a comment is not a mechanism (the B43 drift trap). Change the fill and the check would either break or, worse, keep passing against a colour nothing renders any more. The atlas is now the single owner. **Evidence — three runs on identical geometry, with terrain temporarily given a 2-colour shaded tile to simulate §5 face shading landing:** (A) palette net + Nearest -> `palette=2 sampled=4118400 blended_px=0 PASS` — survives art, which was the entire point of B41; (B) single-constant net + Nearest -> `palette=1 blended_px=456681 FAIL` — what the OLD check does to art it has never seen: a loud false accusation against an innocent setting, exactly the decay mode this entry warned about; (C) palette net + Linear -> `palette=2 blended_px=105946 FAIL` — **the control that matters for trusting (A): widening the net did NOT cost the check its teeth.** Steady state re-verified after reverting the experiment: `palette=1 blended_px=0 PASS`, and `tools/run_checks.sh` green both headless and `--render-checks`. No new check added — this is covered by the existing `FILTER_CHECK`, whose output line now carries `palette=N` as evidence the net is real rather than empty; an unreadable atlas fails explicitly on `reason=empty_palette` rather than as millions of mysterious 'blended' px. **Residual, deliberately not fixed:** fallback (2) (known-answer test at a fixed sub-pixel offset) stays unbuilt — it is only needed once the atlas is dense enough that a Linear blend of two texels lands within `COLOR_EPSILON` of some third texel, at which point membership stops discriminating. Run (C) is how you would detect that: if it ever passes, the net has gone slack. Re-measure then; do not assume. **Process note:** the code comment recording (B) originally carried an invented figure (214,598) written before the run — the B31 sin in miniature, caught only because the measurement was actually taken. The real number is 456,681. | resolved |
| B40 | 2026-07-17 | 2 | **Windowed rendering on this box has collapsed to ~1 fps, and it is environmental, not a code regression.** 60 frames took **59.2s** windowed vs **0.8s** headless. Confirmed not ours: the same measurement on the last known-good commit (**c9fc950**, verified passing windowed earlier today) reproduces exactly — 59.4s / 60 frames, `SIM_CHECK actual_ticks=37 FAIL`. Not contention either: GPU busy **7%**, loadavg **0.60**, no lingering `godot4` processes. The frame loop is blocking ~1s/frame on an idle machine, which points at window presentation/vsync in this non-interactive session (`DISPLAY=:0`) rather than at the game. Consequence: **windowed `SIM_CHECK` now reports FAIL for a reason that has nothing to do with the sim** (34-37 ticks where ~220 are owed) — the clock is honestly reporting a starved loop per §1.4 (if the sim cannot hit rate, the game slows down), and B17's catch-up cap is doing its job. | **Headless is the timing harness; windowed is for correctness only.** Headless is unaffected (`SIM_CHECK PASS`, 181 ticks, 0.8s/60 frames) and is what CI runs, so nothing is blocked. Do not chase windowed tick-rate failures on this box until the presentation issue is understood — and **do not "fix" them by loosening `SIM_CHECK`'s drift tolerance**, which would blind the one check that catches real sim-loop regressions. Framebuffer correctness checks (`FILTER_CHECK`, `OVERLAY_CHECK`) are frame-rate independent and still pass and exit 0 in this state, which is why B22 could be closed empirically despite it. Worth a look by whoever owns the box before anyone reads a windowed perf number as a real signal — it also invalidates windowed FPS as evidence for §7 Q2's 20k-sprite budget (B8), which will need headless or a healthy display. **ROOT CAUSE CONFIRMED 2026-07-17: vsync, and B40+B47 are the SAME cause.** With no compositor presenting on this non-interactive `DISPLAY=:0`, each swapchain present blocks ~1s. One flag settles it. **Measured, same box, same minute, idle (loadavg 0.53, GPU 4%, 32 cores, no competing Godot process — so contention is RULED OUT, not merely doubted):** 60 frames windowed **59.64s** -> with `--disable-vsync` **1.00s** (60x); headless control 1.10s. `SIM_CHECK` windowed **actual_ticks=42 FAIL** -> `--disable-vsync` **actual_ticks=188 PASS**. `ENGINE_CHECK_2_STRESS` (20k sprites) **7.5 fps FAIL** -> `--disable-vsync` **2891.8 fps PASS**. `run_checks.sh --render-checks` wall time ~40-60s -> **3.72s**, green. **NOTHING WAS LOOSENED, and nothing needed to be: `SIM_CHECK` was right the whole time.** It honestly reported a loop starved by a 1s/frame block, exactly per §1.4, with B17's catch-up cap doing its job. Removing the environmental blocker takes it from 42 ticks to 188 ticks — full rate — with its drift tolerance untouched. This entry's own instruction ("do not 'fix' them by loosening SIM_CHECK's drift tolerance") turned out to be the load-bearing call: the check was the messenger. **Reproduction recipe / bound:** symptom is any windowed Godot run pacing at ~1 fps on an idle box while headless is normal. Diagnose in two commands: `/usr/bin/time tools/godot4_mono/godot4_mono --path game --quit-after 60` (expect ~60s) vs the same with `--disable-vsync` (expect ~1s). If the second is fast, it is presentation/vsync, not the game, and no timing number from a vsync-on windowed run means anything. **Fix, already applied: `tools/run_checks.sh` passes `--disable-vsync` on the windowed path.** It is a correctness requirement there, not a perf tweak — and it does not affect what the checks measure, since `FILTER_CHECK`/`OVERLAY_CHECK` read framebuffer CONTENT while vsync governs presentation timing only. On a real desktop with a working compositor, vsync-on would cap at refresh rate (normal); ~1 fps is pathological and specific to nothing being there to present to. **This entry's hypothesis ("points at window presentation/vsync") was correct**; B47 independently hypothesised pipeline-compile stall or agent contention for the identical symptom and was wrong. Two entries, two theories, one cause — worth remembering that a plausible cause agreed on by nobody in particular is still just a guess until one flag tests it. | resolved |
| B44 | 2026-07-17 | 2 | **`main-gd-missing-filter-check` (scout-reported) — NOT REPRODUCIBLE; no commit on master was ever broken.** Report was that `main.gd` fails headless load because `preload("res://scripts/filter_check.gd")` hits a missing file. Verified every commit is internally self-consistent: `930e33c` has no `filter_check.gd` and main.gd does not preload it; `c9fc950` has the file present AND preloaded; `f5732eb` renamed it to `render_checks.gd` and updated the preload in the SAME commit (pathspec included both). A **clean clone of master at `f5732eb`** boots headless to `ISO_CHECK PASS` + `SIM_CHECK PASS`, exit 0. So master has never had the broken combination described. | No code change — there is no defect to fix. Cause is a stale/dirty working tree on the reporting side: the two states that produce this error are (a) a tree holding `main.gd@c9fc950` after `filter_check.gd`'s deletion was pulled, or (b) a scan of a transient worktree — note **a `/tmp/iso-baseline` worktree at c9fc950 existed briefly** during B40's environmental bisect and has been removed, which is a plausible source of a stale sighting. **Fix on the reporting side: `git pull` + re-run; verify with a clean clone before filing a load failure against a commit.** Recorded rather than dropped so the name does not get re-raised. Standing lesson: a report of the form "commit X is broken" is cheap to settle definitively with `git ls-tree` + a clean clone, and expensive to settle by argument. | resolved |
| B43 | 2026-07-17 | 2 | **`iso_depth_check.gd` re-declares the iso constants and transform privately instead of using `Iso` — the exact drift `iso.gd` exists to prevent.** The scout's Q1 proof (`game/scripts/iso_depth_check.gd`, commit `1f2dc9b`) defines its own `TILE_W := 64.0`, `TILE_H := 32.0`, `BIG := 1000.0`, its own `_world_to_screen`, and its own `_depth_far_corner` — all duplicates of `Iso.TILE_W/TILE_H`, `Iso.world_to_screen`, `Iso.depth_key`. The proof is correct and its `BIG` happens to match `Iso.depth_key`'s `1000.0` **by luck, not by construction**. The failure mode is silent and nasty: change `Iso`'s constants or depth formula and the Q1 proof keeps passing against the OLD formula, so the artifact that certifies our depth key stops tracking the depth key we actually ship. `iso.gd` explicitly warns "do not re-declare TILE_W/TILE_H/TILE_Z in a renderer script — the moment a second file owns a copy they drift silently"; this is that. | Not blocking and not urgent — the proof is standalone, is not on the slice path, and is correct as of today. Deliberately **not** edited here: it is the scout's deliverable, cited in their Q1 answer, and rewriting another role's proof mid-flight risks invalidating a result the PM is reading. Fix is mechanical when someone touches it next: `const Iso = preload("res://scripts/iso.gd")` and delete the four local copies, keeping `_depth_origin_only` (that one is *supposed* to be a wrong-on-purpose control). Live counterpart already exists and is correct: `entity_layer.gd` drives its draw order from `Iso.depth_key` and negative-controls it via `DEPTH_CHECK`. **FIXED 2026-07-17 (scout).** `game/scripts/iso_depth_check.gd` now does `const Iso = preload("res://scripts/iso.gd")`; deleted the private `TILE_W`/`TILE_H`/`BIG`/`_world_to_screen`/`_depth_far_corner`, replaced their call sites with `Iso.world_to_screen(...)` and `Iso.depth_key(...)`. Kept `_depth_origin_only` and `SCREEN_OFFSET` (the latter is a test-scene viewport-placement offset, not a projection constant — `Iso.world_to_screen` is origin-relative and can return negative coords with no camera present). Re-ran the proof windowed (screenshot-sampled, same as always — this check has never been headless-runnable): `ENGINE_CHECK_Q1_FARCORNER result=PASS naive_top=green correct_top=red bug_reproduced=true bug_fixed=true`, byte-identical result to before the port. Now tracks `Iso.depth_key` by construction, not by luck. | resolved |
| B46 | 2026-07-17 | 6 | **Implement D21's two amendments to the `sim.tick` payload** (client-consumer ruling; I wrote D21, so I implement it). `stockDelta` (deltas-since-emit, server-global `_primed` baseline) -> `stock` (absolute levels); `beltDeltas: []` -> `null`. Plus `adapter-contract-v0.md` §3 defined the shapes but the implementation still emitted the old ones, so doc and impl disagreed **by conflict**, not merely by omission — B45's closing note is now resolved in the other direction. | **Done, all green.** `SimTickService.Stock()` returns absolute levels and holds NO state (`_lastOre/_lastPlate/_lastGear/_primed` deleted — the method got simpler, which was the point: there is nothing left to get out of sync with a client). `BeltDeltas()` returns `null`. `SimHub.SimTickAsync`'s parameter renamed `stockDelta`->`stock` — the anonymous object's property names ARE the wire shape, so the rename is the wire change. Probe expectations rewritten and **exactly inverted**: the old assertions proved delta-ness (`ironGear < 50`), the new ones prove level-ness (never decreases; last > first; `>= seconds*2`, since ~5 gear/s absolute is ~5N after N seconds where a 20 Hz per-emit delta would be ~0.25 and round to 0-1). The two shapes are therefore not confusable by accident, and each assertion set is the other's negative control. **Verified live: 24/24 assertions PASS, exit 0, including hash parity** (adapter tick=1044 hash=0xe796b3a266fa3c9c == direct `Forge.Sim` replay — the amendments do not perturb the sim, as expected since none of this touches sim state). **NEGATIVE CONTROL, per the standing convention: run against the pre-amendment shapes the probe FAILS with exit 1 and 3 named assertions** (`payload has 'stock'`; `beltDeltas is null on every emit`; stock value assertions skipped) — observed payload `{"beltDeltas":[],...,"stockDelta":{...}}`. It discriminates. **Also fixed a real probe defect found doing this:** a wrong-shaped payload made it reach `GetProperty("stock")` on an absent field and die with an unhandled `KeyNotFoundException` -> **exit 134**, breaking the probe's own documented contract ("1 = a contract violation") and reading in CI as an infrastructure flake rather than the contract breach it is. Value assertions are now guarded behind field existence, so a wrong shape FAILs loudly and exits 1. Scope respected: `game/` untouched (scout's tree lock). | resolved |
| B48 | 2026-07-17 | 6 | **Shared-box hazard: concurrent agents collide on the adapter's port, and `dotnet run --no-build` can serve a stale binary that silently invalidates a test result.** Hit twice while verifying B46. (a) `adapter/` defaults to `http://127.0.0.1:5000` and another role already held it, so a plain `dotnet run` dies with `AddressInUseException`. (b) Worse and much more dangerous: for a stretch, port 5299 served payloads (`stockDelta` + `beltDeltas:[]`) that **no source in the tree could produce** — the running process predated the rebuild. A probe pointed at a stale binary reports a confident, precise, wrong answer, and the failure looks exactly like a real contract breach. Root causes: `dotnet run --no-build` trusting whatever is in `bin/`, plus an aborted command chain (a `pkill` returned 144 and killed the chain **before** its `&& dotnet build`, so a "rebuild" I believed had run never did). | **Convention, no code change.** (1) **Never `pkill -f` on a pattern that can match another role's process** — `pkill -f "Forge.Factory.Adapter"` would have killed the other agent's adapter on 5000. Kill by PID, obtained from `ss -lptn 'sport = :PORT'`. Verified port 5000 stayed up throughout. (2) **Run your own adapter on your own port** (`ASPNETCORE_URLS=http://127.0.0.1:5299`) rather than contending for 5000; the probe already takes a hub URL argument, so this costs nothing. (3) **Before trusting a probe result, confirm the binary is actually yours** — `stat -c %y bin/Release/*/adapter.dll` against the clock, or rebuild clean (`rm -rf bin obj`). A green probe against a stale binary is worse than a red one. (4) Do not chain `pkill` with `&&` — a non-zero pkill silently skips the rest of the line. Cheap habits; each of these cost real time this session. | open |
| B55 | 2026-07-17 | 6 | **Wire SimHubClient into the main scene, receive side only** — GDScript sink + on-screen indicator, C# frozen (scout mid-B53 in that file). Three gaps found, none blocking; landed what works per dispatch. **(a) THE HARNESS BINARY CHANGED, and the failure mode is silent.** main.tscn now references `res://SimHubClient.cs`, so it only loads fully under `tools/godot4_mono/godot4_mono`. Under plain `tools/godot4` (no .NET support) the scene prints `No loader found for resource: res://SimHubClient.cs` + a Parse Error, **drops the SimHubClient and SimState nodes, runs the remaining checks, and still EXITS 0**. So a CI or a human on the old binary gets a green run with the sim feed silently absent — errors on stderr, success to the exit code. **(b) No reconnect in the frozen C# surface:** `ConnectAsync()` is called once in `_Ready`, there is no `.WithAutomaticReconnect()`, and nothing retries. Adapter down at boot = disconnected forever, even after it comes up; a mid-run adapter drop is permanent. **(c) No connection-state signal:** `Connected` is a plain property, so the sink must poll it (4/sec here). | **Landed; all six checks green with adapter both UP and DOWN, exit 0 either way.** Live feed proven end-to-end: `SIM_FEED first tick=978 belts_modelled=true stock={ironOre:0, ironPlate:1, ironGear:7}`. Adapter-down is a normal state, not an error path: one `connect failed` line, no spam, indicator reads `sim: disconnected`. (a) **Note for whoever owns CI: the command for main.tscn is now `tools/godot4_mono/godot4_mono --headless --path game`.** Plain `tools/godot4` is no longer sufficient and, worse, will not tell you — exit 0 is not evidence the scene loaded. Worth a CI assertion on the absence of `No loader found` in output, since the exit code cannot carry it. (b)/(c) are the scout's surface (B53) and were NOT edited per the tree lock. Neither blocks this unit: the indicator correctly shows `sim: disconnected` and the sink polls. (b) is worth fixing in B53 or a follow-up — `.WithAutomaticReconnect()` plus a reconnected signal is the fix, and until then "start the adapter after the game" simply never connects, which will read as a bug to anyone who tries it in that order. Also note the sink deliberately reads `BeltsModelled` **dynamically** rather than hardcoding D21's "v0 = belts unmodelled": B54/D22 landed mid-unit and belts ARE now modelled (`beltDeltas` is an array), so a hardcoded v0 assumption would already be wrong. | resolved |
| B57 | 2026-07-17 | 6 | **The check harness could not fail. `tools/godot4` (standard build, no .NET module) run against `main.tscn` prints `No loader found for resource: res://SimHubClient.cs` + a Parse Error, silently DROPS the `SimHubClient` and `SimState` nodes, runs the surviving checks, and EXITS 0.** Every remaining check passes, so `$?` reads green to CI, to a script, and to a human skimming — while the sim feed is simply not in the scene. Root cause is structural, not a bug in Godot: **a dropped node cannot fail a check it never ran**, so absence of a check is indistinguishable from success at the exit code. Introduced by B55 attaching a C# script to `main.tscn`; latent for anyone still on the pre-B55 command. Compounding it, `docs/mono-conversion-notes.md` step 4 carried a B51-era measurement stating the standard build "runs `main.tscn` fine (nothing in it references a `.cs` script)" — true when written, false since B55, and the exact sentence that would send someone into the trap. | **Fixed: `tools/run_checks.sh` (new file, no tree-lock conflict) is now the canonical invocation, and it asserts on OUTPUT, not the exit code.** Four assertions: (1) no `No loader found` in output; (2) **every expected check actually emitted** — the general form of the trap, since any node that fails to load goes silent, and silence is not success; (3) no `result=FAIL`; (4) exit code, checked LAST because it is the weakest signal here. **Negative-controlled: `tools/run_checks.sh --binary tools/godot4` FAILS (exit 1) on the same tree where the raw binary exits 0.** Two independent assertions catch it — the loader grep AND a missing `SIM_STATE_CHECK` (the canary: it lives on the `SimState` node, which is what disappears) — and the control output is self-documenting, since **`[PASS] godot exited 0` sits right above `run_checks: FAILED`**. Canonical: `tools/run_checks.sh` (headless) / `--render-checks` (windowed); both green. Docs corrected: `mono-conversion-notes.md` step 4 no longer claims the standard build runs `main.tscn`, and step 6 warns that pointing it at a scene that has since gained a C# script yields a green exit and a quietly incomplete scene. **The wrapper earned its keep before it shipped:** its first expected-checks list demanded `SIM_CHECK` in `--render-checks` mode, where B46 deliberately suppresses it (the GPU readbacks starve the frame loop, so the two harnesses cannot share a run) — the wrapper caught my own list being wrong on its first real run. Scope: wrapper + docs only; no C#, sim/, adapter/, and the scout's fetch scripts untouched. | resolved |
| B56 | 2026-07-17 | 2 | **Belt placement SEND path, game-side C# — scout's half of the belt-send huddle.** `belts_for_adapter()` (B52) emits `{cell,dir}` client-side but had no way to reach the adapter. Huddle with the mathematician converged live on the wire contract before either side built: `POST /sim/belts`, raw array body (verbatim `belts_for_adapter()` shape, no wrapper), `202 Accepted` (not 204 — D23: the POST enqueues, the tick loop drains it at a tick boundary, so 204 would claim a completion that hasn't happened) with `{appliedAtTick,accepted,rejected:[{cell,reason}]}`. **Note:** at the time of this entry `/sim/belts` is not yet live on the adapter (mathematician's side of B56, in flight) — confirmed 404 against a fresh, freshly-verified adapter build. | `SimHubClient.cs`: `SendBeltsAsync()` posts the raw array and parses the 202 body via `ParseBeltPostResponse()` (pure, public, unit-tested without a network) — `Accepted` and `Rejected` stay distinguishable per the huddle's own reasoning (a 202 can be simultaneously "transport succeeded" and "sim refused this cell", same shape as `entity_layer.gd:81`'s `place()` returning `-1` for "nothing happened" rather than throwing). Also extended tick parsing to expose the actual `BeltDeltas` array (previously only a `BeltsModelled` bool) — needed so a round-trip check can diff before/after state, not just observe a boolean flip. `SimHubClientCheck.cs` gained three legs: **(6) belt-post parse control** (canned 202 with one accepted, one rejected cell — both must parse out separately); **(7) unreachable-endpoint control** (`SendBeltsAsync` against a dead port returns `Ok=false`, not silently true); **(8) live round-trip** — POST a belt, wait for a `beltDeltas` emit at `tick >= appliedAtTick`, assert it differs from the pre-POST sample. Leg 8 correctly reports `SKIPPED` (not FAIL) while `/sim/belts` 404s — verified against a freshly built, PID-verified adapter instance on a dedicated port, killed by PID after. Full `tools/run_checks.sh` suite green throughout (adapter both up and down). **Residual, explicitly out of this unit per the dispatch:** GDScript/UI hookup (calling `SendBeltsAsync` from `belts_for_adapter()`'s actual caller) is iso's tree, own ID later. **UPDATE 2026-07-17: live round-trip completed.** `/sim/belts` came up in the shared tree (mathematician's side, uncommitted at time of testing) while re-verifying B58 below. Discovered empirically that the world enforces map bounds — `(500,500)`, `(-50,-50)`, and the leg's original `(9000,9000)` all rejected with `reason:"off-map"` against a live probe; `(100,100)` is in-bounds and accepted. Retargeted the leg's test cell accordingly. **`SIM_HUB_CLIENT_CHECK_BELT_ROUNDTRIP result=PASS applied_at_tick=3799 saw_tick=3813 accepted=1`** — a real belt posted, accepted, and a subsequent `beltDeltas` emit reflected it. Full `belts_for_adapter()` → `SendBeltsAsync` → `/sim/belts` → `beltDeltas` loop proven end-to-end. Deliverable 4 of the huddle brief. **SERVER HALF LANDED (mathematician), which is what scout's round-trip ran against** — it was live from the shared worktree but uncommitted at the time, so that proof only became reproducible with this commit. `POST /sim/belts`: raw array verbatim, `202` + `{appliedAtTick,accepted,rejected[]}`, `400` only on a malformed body. `World.ApplyBeltBatch` chains contiguous cells into ONE lane (a posted 3-cell run → a 3-tile belt, `length=196608` Fx32) and reports `OffMap`/`BadDir`/`Occupied`/`DuplicateInBatch` as normal events. Independently re-proven: `{"appliedAtTick":560,"accepted":3}` → a later `beltDeltas` carries `belt 1, length=196608`. Suite 36/36 (7 new `PlacementTests`). See **D23** for the tick-boundary/sort/input-stream ruling and its hash cost. **v0 LIMITATIONS, recorded not hidden:** (1) chains are NOT joined to belts from earlier batches — that means rebuilding a live lane and either discarding its items or migrating them, neither specified — so a cell placed next to an existing belt starts a SEPARATE belt; (2) occupancy is decided at apply time, one tick after the response, so it cannot appear in `rejected[]` — only `off-map` can. Both need follow-ups once the UI lets players extend belts. **B63 reproducibility re-verify (2026-07-17):** the original round-trip ran against `/sim/belts` while still uncommitted in the shared tree, so it was only reproducible from that working tree. Re-ran clean from **committed `origin/master` HEAD `fc5eb01`** (downstream of `7f88f54`/`14db35c`, includes B62's inserters): fresh adapter build (`rm -rf bin obj && dotnet build -c Release`), launched on a dedicated port, PID `104098` confirmed via `ss -tlnp` before trusting it. `SIM_HUB_CLIENT_CHECK_BELT_ROUNDTRIP result=PASS applied_at_tick=1143 saw_tick=1155 accepted=1`. Killed by PID after. Reproducible from a clean checkout, not tied to a particular working tree's uncommitted state. | resolved |
| B58 | 2026-07-17 | 2 | **`SimHubClient` did one-shot `ConnectAsync` with no retry — an adapter started after the game never connected, and a mid-run drop after a successful connect was the only path `.WithAutomaticReconnect()` covered.** Measured pre-fix: `StartAsync()` failing once logged a single `connect failed` line and the client never tried again, permanently. | **Fixed: `ConnectAsync` now retries the INITIAL connect indefinitely** (until the node is freed — a `volatile _disposed` flag checked each iteration and after the catch, so `_ExitTree` stops it cleanly rather than leaving a background loop spinning against a disposed connection). `InitialConnectRetryDelayMs` (default 2000ms, `[Export]`) and a new `ConnectAttempts` counter are exposed so the check can drive/observe it without waiting on real-world backoff. **B53's rebaseline-on-reconnect is untouched** — `.WithAutomaticReconnect()` + the `Reconnected` handler still own the *post-success* drop path; this only changes what happens *before* the first success, a genuinely separate code path (confirmed by reading, not assumed: `WithAutomaticReconnect` never fires for a `StartAsync()` that throws outright). Two new negative-controlled legs in `SimHubClientCheck.cs`: **(9) retry-attempts control** — a permanently unreachable target (300ms retry interval) must show `ConnectAttempts >= 3` and stay disconnected; proves retrying actually happens, not just that failure is silent. **(10) real "adapter started after the game" proof** — a client pointed at the default `HubUrl`, created before any adapter exists, polls up to ~12s; SKIPPED (not FAIL) if nothing ever comes up. **Live-verified for real**, not simulated: bash-orchestrated a run where the check launched with port 5299 completely empty (confirmed via `ss`), then a freshly built adapter was started ~6s in — `SIM_HUB_CLIENT_CHECK_LATE_ADAPTER result=PASS attempts_at_connect=1 has_baseline=True`. `attempts_at_connect==1` is correctly *not* required to be `>1`: leg 10 runs late in the check's sequence (after several earlier legs' waits), so by the time it starts, an externally-orchestrated adapter is often already up — a client connecting immediately when the server is reachable is the correct behavior, not a weaker proof; leg 9 is what deterministically proves the retry loop itself works, against a target that is *never* reachable. Full `tools/run_checks.sh` suite green; adapter for the timed test killed by PID from `ss` after. | resolved |
| B59 | 2026-07-17 | 4 | **forge-db drift check — the forge-side item on the wrap-up list.** Scope per dispatch (no more-specific existing note found in plan.md/inventory.md beyond B36/B38, which are point findings, not a general wrap-up scope — checked, dispatch's own scope stands): (1) confirm running forge-api includes B38's fix (`f5eaa229`) and its schema matches migrations, by running not reading; (2) re-verify `seed-recipes.sh` idempotency against current db state; (3) re-verify the adapter's cold-load+checkpoint round-trip. | **(1) B38 fix confirmed live by behavior, not history:** negative control (`partId=999999` → `receive-stock`) still 404s; image `sha256:b93ed4...` matches the rebuild from the B38 session. **Schema check surfaced real drift**, caught by running `forge-db plan`/`verify` (built the harness locally via `docker build`, no `go`/`pg-schema-diff` on host) against the live DB — **not clean**: 144 pending statements (12 new tables incl. `recalls`, `payment_schedules`, `terms_documents`, `sales_order_stages`; 90 `ALTER TABLE`; 40 index builds), all purely additive (zero `DROP`, `DELETES_DATA` hazard never triggered — only `HAS_UNTRACKABLE_DEPENDENCIES`/`INDEX_BUILD`, both about `CONCURRENTLY` index safety, not data loss). This is exactly the backlog B36 flagged as needing "the proper long-term fix... running the full forge-db plan/apply reconciler" — evidently never run since. Given non-destructive + a small dev DB (311 tables, low row counts — every statement completed in single-digit milliseconds) this was in-bound to just fix: ran `forge-db apply --env dev --yes`, all 144 statements applied clean, one previously-unapplied idempotent seed script ran (`0010-identity-roles.sql`, all 13 roles already existed — no-op in practice, confirmed). **`forge-db verify` now exits 0 — "PASS, live DB matches desired state."** forge-api stayed healthy throughout, no restart needed (additive changes only). **(2)** `seed-recipes.sh` re-run twice, before and after the schema fix — idempotent both times, same IDs (3/4/5), zero mutation. Also found and cleaned incidental debris unrelated to drift: an un-deleted duplicate test part (`RAW-00003`, id=6) left over from the B36 debugging session — soft-deleted via the API. **(3)** Adapter `/cold-load` (3 items/2 recipes, unchanged) and `/checkpoint` (204, real write) re-verified against the now-current schema — both correct. | resolved |
| B60 | 2026-07-17 | 2 | **No GDScript-callable surface for the belt-post send path.** B56's `SendBeltsAsync(IEnumerable<BeltPlacement> belts)` takes a C# `record struct` sequence — not a Variant-marshallable type, so GDScript cannot call it at all. iso's upcoming placement UI (B61) needs a surface it can call directly with `belts_for_adapter()`'s actual output, zero reshaping. | **`SimHubClient.SendBeltsFromGodot(Godot.Collections.Array belts)`** — accepts `belts_for_adapter()`'s exact shape verbatim (`Array[Dictionary]` of `{"cell":Vector2i,"dir":int}`; unwraps `Variant.AsVector2I()`/`AsInt32()` internally, no reshaping asked of the caller). Result delivered via a new `BeltsPosted(bool ok, long appliedAtTick, int accepted, Array rejected)` signal, deferred through `Callable.From(...).CallDeferred()` — same main-thread-safety pattern as every other callback in the file, since `SendBeltsAsync`'s `await` can resume off the main thread with no guaranteed `SynchronizationContext`. `ok=false` (request itself failed) is kept distinct from `ok=true, accepted=0` (reachable, every cell rejected) — collapsing those would make "the endpoint is down" indistinguishable from "everything you asked for was invalid". **Negative-controlled check FROM GDScript, not C#-to-C#** (the distinction the dispatch asked for): new `game/scripts/sim_hub_belt_bridge_check.gd` + `scenes/sim_hub_belt_bridge_check.tscn` instantiates `SimHubClient` via `preload(...).new()` (standard GDScript/C# interop, not requiring `class_name`/the global class cache per B15's lesson), calls `SendBeltsFromGodot` with the off-map cell `(500,500)` (B56's empirical bounds finding — a cheap, deterministic control since it must always reject), and asserts the marshalled-back `rejected` array actually carries `reason="off-map"`. **Live-verified against a freshly built, PID-verified adapter** (via `ss`, killed by PID after): `SIM_HUB_BELT_BRIDGE_CHECK result=PASS accepted=0 rejected_count=1 rejected_off_map=true`. Correctly `SKIPPED` (not FAIL) with no adapter reachable. Full `tools/run_checks.sh` and the existing `sim_hub_client_check.tscn` suite re-ran green throughout, adapter both up and down. **Scope held**: no placement UI logic touched — that's iso's B61. **B63 reproducibility re-verify (2026-07-17):** re-ran from committed **`origin/master` HEAD `fc5eb01`** (downstream of `7f88f54`, includes B62), against the same freshly built, `ss`-PID-verified adapter (PID `104098`) used for B56's re-verify. `SIM_HUB_BELT_BRIDGE_CHECK result=PASS accepted=0 rejected_count=1 rejected_off_map=true`. Also re-ran the full `tools/run_checks.sh` suite both adapter-up and adapter-down — green both times, now also asserting `BELT_SYNC_CHECK` (iso landed it concurrently, mid-B61; caught the wrapper mid-edit with a transient bash syntax error at one point, self-resolved seconds later as their edit completed — not touched, not this unit's to fix). Adapter killed by PID after. | resolved |
| B61 | 2026-07-17 | 6 | **Placement-UI -> send-path hookup: player belt placements now reach the adapter.** Wires the B52 placement UI to B60's GDScript-callable bridge (`SendBeltsFromGodot` / `BeltsPosted`), which had already landed (`7f88f54`) by the time this started — so no scaffolding-around was needed and the real call went in directly. B60's API surface matched what was needed exactly; **no seam disagreement to record.** | **Done, all six checks green, live round trip proven.** New `game/scripts/belt_sync.gd` is the seam between `entity_layer.gd` (owns belts; its header says it must not know the sim exists, and still doesn't — it emits `belts_placed` and does not care who listens) and `SimHubClient.cs` (owns transport; knows nothing of placement). **WHEN:** on placement commit — once per single-click, once per drag-run release. `place_run()` emits ONCE for the whole run, not per belt: a 20-belt drag is one player intent and must be one POST, not 20 racing requests. Also flushes on `BaselineEstablished`, because belts placed while the adapter was down would otherwise sit unsent until the player happened to place again — "the adapter came up" must mean "the board is synced". **WHAT:** only `pending_belts()` (never-sent), never `belts_for_adapter()` (the whole board). Send-state is three-valued (LOCAL/SENDING/SENT), not a bool — SENDING is the one that matters: without it a placement mid-flight re-posts the first batch and the adapter applies it twice. **REJECTED:** removed locally AND their cell freed — a belt drawn where the sim has none is the ghost lying, and leaving the cell claimed would block re-placement with nothing on screen explaining why. `ok=false` (request failed) vs `ok=true, accepted=0` (adapter refused) are kept distinct exactly as B60 separates them: the first returns belts to LOCAL for retry, the second deletes them. **EVIDENCE — live, adapter up: `Applied belt batch at tick 888: 2/2 accepted, 3 belts now`** (adapter log) with the client having posted its pending belts on baseline. `BELT_SYNC_CHECK incremental=true board_control_caught=true reject_removes=true adapter_down_loud=true silent_control_caught=true result=PASS`, wired into `run_checks.sh`'s expected list; green headless and `--render-checks`. **Negative controls:** (1) incremental — after 2 sent + 1 new, `pending==1` while `belts_for_adapter()==3`; the board count is the control, and if the two agreed the assertion would not be testing incrementality at all. (2) adapter-down — belts must stay unsent AND say so; asserted the unsent count is UNCHANGED rather than merely "no crash", since a sink that silently marked them sent would report 0 unsent, which is the exact bug this leg exists to catch. **v0 limitation respected, not fixed:** later batches do not join existing belt chains, so the UI implies no such thing — no "connect to that lane" affordance, and nothing merges a new batch into a previously-sent run. Adjacent placements give two independent lanes, which is what the sim actually builds. | resolved |
| B65 | 2026-07-17 | 6 | **Render belt items from the sim state sink.** Wire `beltDeltas` (ABSOLUTE per D21/D22) into visible items on placed belts, depth-sorted through `iso.gd`. | **PARTIAL — the position math landed and is proven; the DRAW is blocked on B66 (a wire gap), so no items are on screen yet.** Landed in `game/scripts/belt_items.gd`: full run-expansion of the §3.3 encoding (a lane is a list of RUNS, so a packed 80-item lane arrives as ONE `{head,len,item}` and expands backwards `head, head-s, head-2s, …`), Fx32 (Q16.16) -> tiles conversion, null/[]-safe parsing, lane-position -> world point via `Iso` (no second copy of the transform), and depth-sorted draw. **NO EXTRAPOLATION between 20 Hz emits, deliberately**: dead-reckoning would look smoother and be a lie — it would show items advancing along a stopped or backed-up belt, which is precisely the state §6.1 needs the player to SEE. A still item on a saturated belt is information, not a glitch. **LIVE EVIDENCE against a real adapter: `BELT_ITEMS_FEED lanes=6 items=80 renderable=0`** — 80 real items parsed from live emissions, zero placeable, because of B66. `BELT_ITEMS_CHECK run_expand=true expand_control_caught=true saturated_80=true null_ok=true empty_ok=true no_extrapolate=true geometry_control_caught=true result=PASS`, in `run_checks.sh`'s expected list, green headless and `--render-checks`. **Negative controls:** run-expansion vs a spacing-ignoring impl that would stack all items at the head (`[4,4,4]` vs `[4.0,3.75,3.5]`); and the geometry leg — supplying geometry makes the same lane renderable, so the "0 renderable" report is a missing INPUT, not a broken renderer masking as one. The check reports the block LOUDLY rather than drawing nothing quietly, per B57: a renderer that draws zero items and says nothing is indistinguishable from a working one on an empty belt. `set_lane_geometry()` is the single wiring point once B66 lands. | blocked-on-B66 |
| B66 | 2026-07-17 | 6 | **BLOCKER FOR THE MATHEMATICIAN — `beltDeltas` identifies lanes by an opaque index, and no endpoint maps that index to world cells, so a client cannot draw the items it is told about.** The emission is `{"belt":0,"lane":0,"spacing":16384,"length":1310720,"runs":[…]}`. `belt` is an integer. **Searched exhaustively; the mapping exists nowhere on the wire:** `/sim/state` publishes tick/hash/buffers/machineState/inserters and **no belt geometry**; the adapter's only other endpoints are `/health`, `POST /sim/belts`, `/cold-load`, `/checkpoint`; and `POST /sim/belts` takes cells IN (`{"cell":{x,y},"dir"}`) but returns `{appliedAtTick, accepted:int, rejected[]}` — `accepted` is a **count**, not the indices assigned. So cells go into the sim and never come back out. **Measured live:** a client-posted batch of 2 belts appears as `belt:1` and `belt:2` with `length:65536` (1 tile each) alongside the seeded `belt:0` (`length:1310720`, 20 tiles) — the items are all there, unplaceable. | **Landed the half that is unambiguous; did NOT redesign the wire, and did NOT guess.** Explicitly rejected: inferring index<-cell by replaying the adapter's `ApplyBeltBatch` (Y,X,Dir) sort and assuming sequential assignment. That is sim logic in the client (forbidden by this unit's brief), and it breaks **silently** on a partially-rejected batch, on any pre-seeded belt (there is one — `belt:0` is not from the client), or on any sim-side reordering. A wrong-but-plausible mapping draws items on the wrong belts, which is worse than drawing none. **Requested fix, cheapest first: add the lane's cells to each `beltDeltas` entry** (e.g. `"cells":[{x,y},…]` in travel order, or `origin+dir+len`). **The principled argument is D22's own:** D22 ruled belts absolute so that "a late-joining client can reconstruct belt contents from any single emit rather than needing a baseline it has no way to obtain" — but without geometry a late-joining client can reconstruct *what* is on a belt and never *where it is*, so the stated goal is only half-met. Geometry per entry keeps that property intact and is self-contained (works for seeded belts too, survives resync, needs no POST-response correlation). **Alternative, weaker:** return assigned indices in the `POST /sim/belts` response — but that only covers client-placed belts, leaves seeded ones unrenderable, and forces the client to hold a correlation table across reconnects, i.e. exactly the baseline D22 was designed to avoid. Cost either way is a few fields; `belt_items.gd:set_lane_geometry()` is the one wiring point waiting for it. | open |
| B68 | 2026-07-17 | 6 | **Assigned "B66" by the dispatch — collided with an already-uncommitted, actively-referenced B66 (the belt-geometry wire gap above, cited by B65's own "blocked-on-B66" and `set_lane_geometry()` notes). Moved per the pinned convention (fewer references moves; zero references here vs. two live ones there) — also skipped B67, which the dispatch itself flagged as concurrently reserved for the mathematician's B67 work, to avoid a second near-certain collision.** Task: at HEAD `6f04f99`, verify the checkpoint round-trip through forge-api persistence with `Sim:Inserter=true` — save a checkpoint from a running inserter world, restart the adapter, restore, confirm the state hash is byte-identical pre/post restore; default-world (no inserters) as the negative control. VERIFY-ONLY, no edits to `sim/`/`refsim`/`adapter/`. | **Round-trip FAILS — not a bug, a capability that does not exist.** Read every persistence-adjacent path in `adapter/` and `sim/Forge.Sim/`: `ForgeApiClient.cs` has exactly one write path, `WriteCheckpointDeltaAsync` (an aggregate stock delta → `receive-stock`/`use-stock`, unrelated to World state); `Program.cs`'s only `checkpoint`-named route is that same delta writer plus a `sim.checkpointed` hub ping carrying the current tick — it does not serialize anything. `World` (`sim/Forge.Sim/World.cs`) has `Hash()`/`HashHex()` but no `Serialize`/`Export`/`ToJson`/restore counterpart anywhere in the sim project. `SimTickService`'s `_world` field is constructed fresh (`new World(content, ...)`) on every startup with no attempt to load prior state from forge-api or anywhere else. **Confirmed empirically, not just by reading** (claims get RUN): started the adapter with `Sim__Inserter=true` on a dedicated port, let it run to tick 1298 (hash `0xd9c07069dab5830d`, inserters `[Blocked/holding, Swinging/holding, Swinging/holding]`), called the only `/checkpoint` endpoint that exists, killed the process by PID (via `ss -lptn`), restarted identically — post-restart: tick **443** (a fresh count, not a resumed one), hash `0xdf5793365ec3adc7` (different), inserters reset to `[Swinging/holding, Idle, Idle]` (does not match pre-restart state). **Negative control (default world, no inserters), same cycle:** pre-restart tick 807/hash `0x5d77cc4c40efc223` → post-restart tick 203/hash `0x6698ccc9d0fed26e` — same "no restore" behavior, confirming this is general (no adapter-persisted World state at all, not an inserter-specific gap), and `inserters: null` correctly held on both sides of the restart (D21's rule intact). No proposed fix — out of scope for a verify-only unit and squarely sim/adapter territory (the mathematician's tree, concurrently in use for B67). If this capability is wanted, it needs a real design: what to serialize (the whole `World`? belts+inserters+buffers+tick, enough to reconstruct `Hash()`?), where it lives given D10 (forge-api has no generic blob-store endpoint today — `receive-stock`/`use-stock` cannot carry it), and a restore-on-startup path that does not exist in any form now. | open |
| B22 | 2026-07-17 | 2 | **Deferred verify: zoomed-panning shimmer is unverified.** `project.godot` now sets `textures/canvas_textures/default_texture_filter=0` (Nearest) per `iso-review-scaffold.md` B2, to protect the pixel-perfect 2-across/1-down diagonals §1 chose dimetric 2:1 for. But the failure it prevents — diagonal edge shimmer — **only manifests in motion**, and there is no camera controller yet (the scaffold's `Camera2D` is static), so the check is not yet runnable. A static screenshot cannot show it. | Static windowed boot check accepted as sufficient for now: Vulkan Forward+ on the RX 9070 XT boots clean, no script/scene errors, `SIM_CHECK PASS`. The motion check is deferred until pan/zoom lands (`iso-review-scaffold.md` A2 checklist) — at which point it is a 30-second look: pan diagonally at 0.5 and 0.25 zoom and watch the tile edges. **Cheap now, and it is the only thing that proves the Nearest setting actually took**, so it should ride along with the camera work rather than being scheduled separately. **RESOLVED 2026-07-17 with the A2 camera work, but NOT as the eyeball check described above — that check was rejected as unrunnable and replaced with a stronger one.** Shimmer is a *perceptual symptom*; judging it by eye is unrepeatable, unavailable to CI, and unavailable to an agent with no eyes on the window. So `game/scripts/filter_check.gd` tests the *mechanism* instead: under Nearest the sampler returns a verbatim texel, and the atlas holds exactly two colours (fill + transparent), so every on-screen pixel MUST be either the fill or the background — intermediates are unreachable. Under Linear, diagonal edges blend and emit colours in neither, and those intermediates *are* the shimmer churning sub-pixel. It samples a 220x130 region (away from the antialiased `CanvasLayer` label, whose text would otherwise contribute blended pixels unrelated to the tile sampler) after forcing the sampler off the texel grid with a fractional camera offset at a minifying zoom stop — without that, Nearest and Linear coincide and the test passes vacuously. **Result (windowed, RX 9070 XT): `FILTER_CHECK sampled=114400 blended_px=0 PASS`. Negative-controlled per the B18 lesson — flipping the setting to Linear yields `blended_px=964 FAIL`, so the check discriminates rather than always passing.** | resolved |
| B15 | 2026-07-17 | 2 | GDScript static analysis in 4.7 treats "variable type inferred from Variant" (e.g. `var x := abs(a - b)` and `var x := "A" if cond else "B"`) as a hard parse **error**, not a warning, under this project's default strictness — first headless run failed to even load the script. **(Renumbered from a colliding B13 — same concurrent-write collision as B14 above.)** | Added explicit type annotations (`var diff: float = ...`, `var result: String = ...`) instead of `:=` inference on `abs()` results and ternaries. Worth remembering for all future GDScript in this project — inference-heavy style will keep tripping this. | resolved — ratified by PM 2026-07-17; scout-assigned numbering kept as-is **CONVENTION EXTENDED 2026-07-17 (B15-family): shared GDScript uses `preload`, never `class_name`.** The editor class cache does not exist on a fresh headless checkout, so a `class_name` reference parse-errors the **whole scene tree** before any check runs — i.e. it fails at the moment and in the environment where diagnostics are thinnest (CI, a clean clone), not on the author's machine where the cache is warm. Same root as B15's strict Variant typing: GDScript's editor-time conveniences are not runtime guarantees. |
| B27 | 2026-07-17 | 3 | **PM-requested write-path verification (orchestrator called this "B13" — already taken by the scaffold's GDScript-parse-error entry above; same collision pattern as B14/B15, renumbered here).** All live verification up to this point was read-only (Engineer/kiosk JWT + `GET /api/v1/parts`). Needed proof the contract's §5/§6 write endpoints actually work with the same identity before building the checkpoint path on top of them. | Hit `POST /api/v1/inventory/receive-stock` live with the adapter's own kiosk identity (`GAME-ADAPTER-01`, Engineer role) — **204, and on-hand quantity confirmed moved (10 → 15 across two calls)**. Required capability `CAP-INV-CORE` was already enabled; no data change needed. Confirmed via `factory/adapter`'s own `ForgeApiClient.WriteCheckpointDeltaAsync` → `POST /checkpoint`, not just curl. §6 Job/ProductionRun lifecycle was not exercised — §5 was sufficient and is what the adapter skeleton actually uses; §6 stays optional/unbuilt per the addendum's own fallback. | resolved |
| B28 | 2026-07-17 | 3 | **WorkCenter (machine) mapping skipped in the adapter's cold-path loader, by name, per instruction.** `GET /api/v1/work-centers/{id}` shape was never curl-verified (survey/contract flagged this as low-risk-but-unverified). Building it out now would cost time the slice doesn't need — v0 recipes carry no machine/category assignment. | `SimRecipe.Category` is emitted as `null` in `factory/data/live-import.json`; `ColdPathLoader` logs the gap into the JSON's own `Gaps` array at runtime, not just here. Revisit when the slice needs machine-gated recipes (post-v0, per `factory-math-v0.md`'s machine-category rule). | accepted |
| B29 | 2026-07-17 | 3 | **Seed DB had zero BOM data.** Both seeded Parts (`ASM-00001`, `RAW-00001`) had `bomLineCount: 0` — no BOM revision existed for either, so a live cold-path pull would have produced items with no recipes at all, which wouldn't demonstrate the D6 Part→item/BomRevision→recipe mapping. | Added one real BOM line live via `POST /api/v1/parts/1/bom` (`RAW-00001` qty 3 under `ASM-00001`) using the adapter's own credential — this auto-created BOM revision 1, which `/cold-load` then fetched for real. Documented in `factory/adapter/README.md`. Not synthetic data injected into the JSON output — it's real forge state, created through the real write endpoint the adapter itself uses. **SUPERSEDED 2026-07-17 by the canonical seed (B35/B36) — the improvised single-line proof stays in place (parts 1/2, untouched) but `/cold-load` now points at the real 3-item/2-recipe chain instead.** | resolved |
| B33 | 2026-07-17 | 3 | **PM re-flagged B13/B27 as still open — re-verified, still passes.** The PM's dispatch was written against a stale view (B27 already closed this same check with real evidence: 10→15 on-hand). Re-ran it fresh against the current `forge-api` container (`Up 9 minutes (healthy)`, restarted since B27) using the same persistent `GAME-ADAPTER-01` credential — `POST /api/v1/inventory/receive-stock` → **204** again, no re-login issues, credential survived the container rebuild (it lives in Postgres, unaffected by API container churn). No new information; B27 stands as the record. | No fallback needed — confirms B27's original result rather than requiring one. Flagging here only so a future reader doesn't waste time re-litigating: check B27 first. | resolved |
| B32 | 2026-07-17 | 4 | **`adapter-contract-v0.md` §2 was stale — still specified an adapter-owned `factory_sim` Postgres schema after D10 dropped it.** The contract's Postgres-schema section was written *before* D10 amended D6 (own-tables → HTTP-only), and was never reconciled afterward. The PM's next dispatch read the contract literally and asked for "the factory_sim schema migration" as part of "implementing contract v0" — a reasonable ask given what the doc said, but one that would have reversed an already-ratified decision (D10) if built as asked. | Rewrote §2 to mark the Postgres schema sketch as historical/superseded, point at D10 as the current design, and confirm `factory/adapter` already implements the HTTP-only checkpoint path D10 specifies (`ForgeApiClient.WriteCheckpointDeltaAsync`, live-verified in B27/B33). **No Postgres migration was built — building one now would contradict D10, not extend the contract.** If a real reversal is ever wanted, it needs its own decision entry undoing D10, not a doc patch. | resolved |
| B35 | 2026-07-17 | 4 | **Closes B29 for real: canonical `recipes-v0.toml` content seeded into live forge, `/cold-load` now emits the actual chain.** B29's improvised single BOM line proved the pipe but didn't match the canonical set. | Created `factory/adapter/seed-recipes.sh` (idempotent — finds-by-name before creating, HTTP-only per D10, uses the adapter's `GAME-ADAPTER-01` identity) seeding 3 new Parts (`factory-v0-iron-ore` id=3 Raw/Buy, `factory-v0-iron-plate` id=4 Component/Make, `factory-v0-iron-gear` id=5 FinishedGood/Make) and 2 BOM lines matching the TOML exactly: 1 ore→1 plate (`smelt-iron-plate`), 2 plate→1 gear (`craft-iron-gear`). Updated `adapter/appsettings.json` `ColdPath:PartIds` to `[3,4,5]`. Re-ran `/cold-load` live: **3 items, 2 recipes**, `live-import.json` now matches the canonical ratios exactly (committed). Forge's model still can't carry duration/speed/machine-category (per B28 — no field for it), so those stay TOML-only; the sim reads the TOML as truth per D14, this is the forge-linkage proof, not a competing source. B19 checked non-conflicting (this seed is a DAG pushed OUT to forge, not real BOMs pulled IN — see B19's own cross-check note). The B29-era improvised line (parts 1/2) is left in place, untouched, just no longer read by `/cold-load`. **Round-trip re-verified against the canonical IDs (2026-07-17): cold-load → checkpoint write (part 3, +5, 204) → adapter restart → cold-load again → state matches (3 items/2 recipes, on-hand 7 survived the restart, unchanged from forge not adapter memory). See B38 for the negative control run alongside this, which found a real forge-api gap (unrelated to this pass/fail).** | resolved |
| B36 | 2026-07-17 | 4 | **Found + fixed: forge-api's own EF-model-vs-schema drift blocked ALL Part creation, not just seeding.** `POST /api/v1/parts` 500'd unconditionally — `CreatePartHandler` succeeds, but the barcode auto-created as a side effect (`BarcodeService.CreateBarcodeAsync`) failed: `column "lot_record_id" of relation "barcodes" does not exist`. This is a real bug independent of the game project — it would block part creation for anyone using this forge-api instance, not just this seed script. | Confirmed via `forge-db/schema/tables/barcodes.sql` that `lot_record_id` (nullable, FK → `lot_records(id)` `ON DELETE CASCADE`) is part of the **declared desired-state schema** — the live DB was simply behind it (the exact drift class forge-api's own CLAUDE.md describes a repair procedure for). Applied the two DDL statements directly (`ALTER TABLE barcodes ADD COLUMN lot_record_id integer;` + the FK constraint) rather than the full forge-db reconciler image build, to stay inside the ≤30 min budget — same net effect, additive/nullable/non-destructive, zero data-loss risk. Re-verified live: Part creation now returns 201. **This is a forge-api-repo bug, not a game/adapter concern** — flagging for whoever owns forge-api next; the proper long-term fix is running the full forge-db `plan`/`apply` reconciler (or adding an EF migration-equivalent) so this doesn't only live as a manual `ALTER TABLE` on one dev DB. **Scope note: this is NOT a D10 violation** — D10 governs the *adapter's* runtime persistence model (no PG client in normal operation); this was a one-time interactive repair of forge's own schema drift, done via psql outside the adapter's code path entirely. | resolved |
| B42 | 2026-07-17 | 4 | **`recipes_to_json.py` restore question — *(ID moved B41→B42 2026-07-17: B41 collided with iso's FILTER_CHECK entry, which is cited in `game/scripts/render_checks.gd:75`; moving this one cost zero reference updates.)*  — the dispatch that raised it calls it "B38", which collides with this log's B38 (forge-api receive-stock partId-existence gap, already resolved/open before this one landed). Filed here as the next free number per the standing convention (proposer takes next free from the log).** Was the accidental over-delete in commit `5dd61a8` (which swept in unrelated pre-staged deletions alongside an `inventory.md` edit) actually a violation of D17's "keep the .py" clause? | **No restore — RULING CHANGED mid-dispatch.** File confirmed still absent (`git log` shows last touching commit is the delete in `5dd61a8`; `ls` confirms not present on disk). Initial instruction was to restore-and-log-as-D17; before that executed, a redirect arrived: the mathematician's C# sim core now ships `Content.Validate` (in `factory/sim/Forge.Sim/`), which owns §2.4 validation with real negative tests, making the Python validator redundant regardless of how it got deleted. **Ruling: stays deleted.** See **D19** (supersedes D17's "keep the .py" half only — the JSON-deletion half of D17 was always correct and unaffected) and **D18** (Tomlyn dropped in favor of `MiniToml.cs`, the reason the C# loader needed its own validator anyway). Resolved-no-restore. | resolved-no-restore |
| B37 | 2026-07-17 | 4 | **PM re-flagged B29 as still open — stale view again (same pattern as B33's read of B27).** B35 already closed B29 fully: canonical seed landed and pushed at `7c37c16`, `live-import.json` committed with the real 3-item/2-recipe set. Re-verification requested nonetheless, including a specific check that the pipeline correctly handles `mine-iron-ore` (the TOML's zero-input extraction recipe, the deliberately-inexact Fx32-remainder recipe per `recipes-v0.toml`'s comments) rather than mishandling or dropping it. | Full live re-run, forge-api healthy (`Up 19 minutes`): `seed-recipes.sh` re-run — idempotent, all 3 parts + both BOM lines found-existing, zero mutation. `POST /cold-load` re-run fresh — **3 items, 2 recipes**, byte-identical to the already-committed `live-import.json` (`git status` shows no diff). **Mine-iron-ore tripwire, checked explicitly:** `factory-v0-iron-ore` (id=3) appears in `Items` with `Raw: true` and correctly has **no** entry in `Recipes` — forge's BOM model has no way to represent a zero-input extraction recipe (a BOM line always has a child part; `mine-iron-ore` has none in the TOML), so the loader correctly emits nothing for it rather than fabricating one. That's structurally the same gap already logged as B28 (no duration/speed_base/category fields either) — the Fx32 remainder-carry math itself (`speed_base=36044`, truncated from `36044.8`) lives entirely in `recipes-v0.toml`/the sim per D14 and was never reachable from forge's side to begin with, so there is nothing here that *could* silently break it. Nothing to fix; nothing changed. | resolved |
| B39 | 2026-07-17 | 4 | **PM re-dispatched B30's three items (adapter_client.gd + guard, sim_clock.gd D13 comment, mono-conversion prep note) as new/outstanding — stale view again, same pattern as B33/B37.** All three already landed and pushed at `72b5e0b`. | Re-verified live rather than redoing: `game/scripts/adapter_client_check.gd` still PASSes against a canned fixture (`ADAPTER_CLIENT_CHECK_B30 result=PASS source=canned-fixture bug_reproduced=true guard_fixes_it=true`); `sim_clock.gd`'s comment already cites D14, not stale D13, and the headless `SIM_CHECK`/`ISO_CHECK` still both PASS; `docs/mono-conversion-notes.md`'s `Forge.Sim.csproj` path reference now matches the mathematician's actually-landed `sim/Forge.Sim/Forge.Sim.csproj` exactly (checked after their C# core commit `04d39a6`, which postdates the note — no drift). Nothing to fix; nothing changed. | resolved |
| B38 | 2026-07-17 | 4 | **Negative control failed: forge-api's `POST /api/v1/inventory/receive-stock` does not validate that `partId` refers to an existing Part.** As part of the B35 canonical-ID round-trip re-verify, ran the required negative control — a checkpoint write against a deliberately nonexistent `partId=999999`, expecting rejection. It returned **204**, same as a valid write, and created a real `bin_contents` row (`entity_type='part', entity_id=999999, quantity=5`) with no FK/existence check catching it. Confirmed via direct query, not inference. | **Not the adapter's bug and not fixed here** — same "found in forge, not adapter scope" disposition as B36, but this one is an application-logic validation gap (missing existence check in the handler), not schema drift, so it wasn't a safe same-session fix the way B36's additive column was. Phantom row deleted (`DELETE FROM bin_contents WHERE id=3 AND entity_id=999999`) to leave the dev DB clean. **Practical mitigation for the adapter:** `ColdPathLoader` only ever passes `partId`s it just fetched from `GET /api/v1/parts`, so this gap is unreachable through normal adapter operation — it only surfaces when something (a bug, a stale config, a bad checkpoint payload from the sim) sends an unvalidated ID. Flagging for whoever owns forge-api next: `ReceiveStockCommand`/`UseStockCommand` handlers should 404 on an unknown `partId` before writing. **FIXED 2026-07-17 — forge-api `f5eaa229`.** Small and safe: `IInventoryRepository.PartExistsAsync` (mirrors the existing `FindLocationAsync` existence check in the same handlers) + a `KeyNotFoundException` (→404) guard added to the top of `ReceiveStockHandler` and `UseStockHandler`, matching the codebase's standing `Features/Parts/*` convention. Negative-controlled per the standing rule: both new tests written and confirmed **RED** against the unfixed handlers first — `ReceiveStock`'s silently didn't throw (matches this entry's finding exactly), `UseStock`'s threw the wrong exception type (`InvalidOperationException`/"no stock on hand" instead of `KeyNotFoundException`/"not found") — then **GREEN** after the fix. Full forge-api suite: 2022 passed, 0 failed. Live re-verified against a rebuilt container (base `docker-compose.yml`, no dev overlay per B21): `partId=999999` on both `receive-stock` and `use-stock` now 404, no phantom row (confirmed by direct query); a real seeded part (id=3) still 204s correctly. `factory/adapter`'s `/cold-load` + `/checkpoint` round-trip re-verified unaffected (3 items/2 recipes, checkpoint write still lands). | resolved |
| B45 | 2026-07-17 | 6 | *(ID: took next free from the log per the standing convention; the dispatch offered B40 then B43 for a different item, but both were already claimed — B40 by iso's windowed-rendering entry, B43 by `iso_depth_check.gd`. Separately: `minitoml-not-general-toml` needs no B-item — **D18 already carries it verbatim as a ratified constraint**.)*  **`adapter-contract-v0.md` §3's `sim.tick` payload is underspecified, and one field has no producer.** (a) **`beltDeltas` has nothing to report** — the v0 sim core models §3.1 rates and §4 buffers only; belts and inserters are explicitly NOT modelled (`golden-v0.md` §3), so there is no belt state to delta. (b) **`machineState` and `stockDelta` shapes are named but never defined** — the contract gives field names, no structure. (c) **Cadence says "render-rate push (throttled from sim-rate; exact throttle is factory-math-v0.md's tick-rate call)" — but `factory-math-v0.md` names no render/push rate.** It pins the sim rate (60) and nothing else, so the contract defers to a decision that was never made. | **Picked the sane reading rather than blocking, per dispatch.** (a) `beltDeltas: []` — the field exists per contract and there is genuinely nothing in it. Emitting invented belt data would make an unimplemented subsystem look implemented, which is worse than an empty array. Revisit when belts land. (b) `machineState: {miners / furnaces / assemblers -> {StateName: count}}` (the §4.2 state tallies the sim already has); `stockDelta: {ironOre, ironPlate, ironGear}` as **deltas since the previous emit**, not levels — "Delta" is in the contract's own field name, and the probe asserts it (a snapshot would grow without bound). (c) Chose **20 Hz** (emit every 3rd tick at 60 Hz), configurable via `Sim:EmitEveryNTicks`. Sane render cadence; keeps the hub off the sim's hot path. All four readings are live-verified by `tools/simprobe` against a running adapter (18/18 assertions). **RATIFIED 2026-07-17 as standing defaults** (orchestrator, on the `2819e66` acceptance): all four — `beltDeltas: []` for v0, `machineState` = §4.2 tallies, `stockDelta` = deltas-since-emit, 20 Hz push — stand as-is **pending any consumer objection**, no re-work. No objection raised. They are defaults, not a contract amendment: `adapter-contract-v0.md` §3 still does not define these shapes, so the doc and the implementation now disagree by omission rather than by conflict. **RESOLVED 2026-07-17 — the doc-omission gap is closed and the consumer objection this entry invited was raised and upheld.** `adapter-contract-v0.md` §3.1 now defines all four shapes and §3.2 defines the resync/gap-detection story; **D21** ratified 20 Hz and `machineState` as-is but amended the other two, and **B46** implemented the amendments (`stockDelta` -> absolute `stock`; `beltDeltas: []` -> `null`), re-verified live at 24/24 probe assertions including hash parity. This entry's own prediction was right on both counts: `beltDeltas` **was** "the one that will actually bite", and each fix **was** one method in `SimTickService`. Doc and impl now agree explicitly rather than by omission. **The one that will actually bite is `beltDeltas`**: it reads as "belts exist and had no changes" when the truth is "belts are not implemented" — a v1 belt consumer cannot tell those apart from the wire. Revisit when belts land; until then each reading is one method in `SimTickService` (`BeltDeltas()`/`MachineState()`/`StockDelta()`) plus `Sim:EmitEveryNTicks`, so changing any of them is cheap and localised. | ratified-as-default |
| B47 | 2026-07-17 | 2 | **`game/` converted to the .NET (mono) Godot build (per `docs/mono-conversion-notes.md`); all correctness checks pass under it, but `engine_checks.tscn`'s 20k-sprite FPS smoke-test (`ENGINE_CHECK_2_STRESS`) now reads 7.5 fps instead of the ~60 fps it measured under B23/the original scaffold.** `project.godot` got `"C#"` added to `config/features` + a `[dotnet]` section, `game/Factory.csproj` added (`Godot.NET.Sdk/4.7.0`, `net8.0`, no `ProjectReference` to `sim/Forge.Sim` yet — see note). One-time `--headless --editor --build-solutions --quit` glue-build succeeded clean. Re-ran the full existing check suite under `tools/godot4_mono/godot4_mono`: `ISO_CHECK`/`DEPTH_CHECK`/`PLACE_CHECK`/`HOVER_CHECK`/`SIM_CHECK` (main scene, headless) all PASS; `adapter_client_check` PASS (canned fixture); `iso_depth_check` (B43's ported Q1 proof, windowed) PASS; `engine_checks` checks 1/3/4/5 (draw order, atlas, overlay, pixel-snap) all PASS. Only check 2 (the FPS smoke-test) reads low. | **Isolated the cause before logging it: it is NOT a mono regression.** Re-ran the identical stress scene on `tools/godot4` (the plain, non-mono build, no conversion applied) at the same moment — **also 7.5 fps**, byte-for-byte the same number. So whatever is suppressing FPS right now affects both builds identically; it did not come from this conversion. Load average was 3.02 on a 32-core box (not obviously CPU-bound) with no other Godot process running concurrently, so the likely cause is either first-frame Vulkan pipeline-compile stall landing inside the 2s measurement window (RADV is explicitly flagged non-conformant/testing-only by its own startup warning) or contention from the other concurrent agent processes' background CPU/IO load — not confirmed further, per the ~60 min bound. **Not blocking the mono-conversion DoD**: the four correctness checks plus every headless check (the actual definition-of-done list) all pass under mono, byte-identical to the pre-conversion build. Re-measure `ENGINE_CHECK_2_STRESS` in a quiet window before trusting it as a perf gate again; do not read today's 7.5 fps as a real regression on either build. **Deliberately deferred, not forgotten:** the `Forge.Sim` `ProjectReference` — `sim/Forge.Sim/Forge.Sim.csproj` targets `net10.0` while Godot's mono build requires the game assembly at `net8.0` (`Godot.NET.Sdk/4.7.0` pins it), and a lower-TFM project cannot reference a higher-TFM one. Wiring the reference in was out of this task's definition of done (project boots green + existing checks pass); flagging the TFM mismatch now so whoever does the actual sim-core integration doesn't discover it cold — likely needs either a `net8.0`-targeted shim/adapter assembly or retargeting `Forge.Sim` down to `net8.0` (checking whether the sim core uses any net9/net10-only APIs is that task's first step, not this one's).  **RESOLVED 2026-07-17 — the 7.5 fps was VSYNC, and this entry's two hypotheses were both wrong.** Same root cause as B40 (see there for the full measurement set). Re-measured in exactly the quiet window this entry asked for — loadavg **0.53**, GPU **4%**, 32 cores, no competing Godot — so **contention is ruled out, not merely unconfirmed**; and the fix is a flag, so the first-frame-Vulkan-pipeline-compile theory is ruled out too. `ENGINE_CHECK_2_STRESS`, 20k sprites, same scene, same minute: vsync on **7.5 fps FAIL** -> `--disable-vsync` **2891.8 fps PASS**. The 7.5 was never a graphics fact; it was ~1 present/sec times the frames it managed. **Bonus, and worth routing: this ANSWERS isometric-design.md §7 Q2 ("can we draw ~20k sprites/frame?") with a real number — 20,000 sprites at 2891.8 fps, ~48x headroom over 60 fps.** B40 said windowed FPS was invalidated as evidence for that budget and B8 would need "headless or a healthy display"; `--disable-vsync` IS the healthy display, so B8 now has a measurement path and a figure. Not editing B8 from here (not my scope) — flagging it for whoever owns the sprite budget. `tools/run_checks.sh` passes `--disable-vsync` on the windowed path; re-measure any perf gate through that, never a bare windowed run. | resolved |
| B51 | 2026-07-17 | 2 | *(Renumbered from a colliding "B48" — a different item, the shared-box port/stale-binary hazard, already held that ID; same collision pattern as B14/B15/B23/B33/B37. inventory.md is authoritative; using B51 per the PM's reassignment.)* **Game-side SignalR client for `/hubs/sim`, built end-to-end.** `game/SimHubClient.cs` (new C# `Node`): connects to the adapter's hub, subscribes to all three §3 events (`sim.tick`, `sim.checkpointed`, `sim.error`), exposes `LastTick`/`BeltsModelled`/`MachineState`/`Stock` as polled properties plus `TickReceived`/`Checkpointed`/`ErrorReceived` signals for GDScript-land. Coded strictly to post-D21/B46 §3.1 text: `stock` absolute levels, `beltDeltas` null-means-unmodelled (not `[]`), no `seq` (gap detection is `tick` deltas per §3.2). Full §3.2 resync/baseline (`GET /sim/state`) is **not** implemented — residual, noted below, not silently dropped. | **Real bug found and fixed, not just documented:** SignalR's `connection.On<T>()` callbacks fire on the connection's own background thread; calling `EmitSignal` directly from one throws `"caller thread can't call emit_signalp()"` — Godot main-thread-only API called off-thread. Fixed with `Callable.From(() => ...).CallDeferred()` around every signal emission/state write in `SimHubClient.cs`. Negative-controlled check (`game/SimHubClientCheck.cs` + `scenes/sim_hub_client_check.tscn`, headless): **(1) parse control** — a contract-exact payload (`beltDeltas: null`) parses to `BeltsModelled=false`; a one-field-mangled payload (`beltDeltas: []`, the exact distinction §3.1 calls out) parses to `BeltsModelled=true` — proves the null-vs-`[]` discrimination is real, not assumed. **(2) wrong-hub-path control** — connecting to a nonexistent path correctly reports `Connected=false`, proving the check can register failure. **(3) live connect** — ran the adapter ad-hoc (`ASPNETCORE_URLS=http://127.0.0.1:5299 dotnet run`, stopped after; did not touch `adapter/` source, per the B46 stay-out-of-those-paths instruction), connected for real, received a genuine `sim.tick` at `tick=4002`. All three PASS; re-ran with the adapter down afterward — leg 3 correctly reports `SKIPPED`, not a false pass. Full existing suite (`ISO_CHECK`/`DEPTH_CHECK`/`PLACE_CHECK`/`HOVER_CHECK`/`SIM_CHECK`, `adapter_client_check`, `bench_fixed_point`) re-ran green under mono, both with the adapter up and down — no regression. `Microsoft.AspNetCore.SignalR.Client` **8.0.29** pinned in `Factory.csproj` (open-source/MIT, matches `net8.0`); see `docs/mono-conversion-notes.md`. **Residual, logged rather than built:** §3.2's full resync contract (`GET /sim/state` baseline-on-connect, explicit gap-jump detection beyond exposing raw `tick`) is not implemented — `SimHubClient` exposes enough for a v0 consumer to observe ticks and derive gaps itself, but does not do it for them. Also not wired: attaching `SimHubClient` into `main.tscn`/any live scene — it exists as a reusable node, proven standalone, not yet plugged into the running game. Both are natural next units, not silently dropped scope. | resolved |
| B49 | 2026-07-17 | 6 | **C# belt implementation (`transport-v0.md`) landed in `Forge.Sim` and gated on the `transport` golden — but mutation testing found THE GOLDEN ITSELF IS NEARLY TOOTHLESS FOR BELTS.** The scenario reproduces byte-for-byte (all 3 scenarios hash-exact, 8 pre-existing hashes unchanged), yet four independent mutations to the belt code left it **fully green**: (1) making the front run unbounded — *the exact `∞` bug that shipped in `factory-math-v0.md` §3.3*; (2) disabling `merge()` entirely; (3) reversing advance to back-to-front; (4) loosening insert spacing to `>= s-1`. Instrumenting the reference sim showed why: across 12000 ticks the fixture's lane **never holds more than ONE run and `merge()` never fires once**, so multi-run ordering and merging are unreachable. The geometry hides the rest — `L/v = 1310720/2048 = 640` and `S/v = 8` are both EXACT, so items land precisely on the lane end whether or not the front run is bounded (`limit = L` and `limit = ∞` are indistinguishable), and every position is a multiple of `v = 2048`, so `tail >= s` and `tail >= s-1` can never disagree. The golden pins throughput, transit latency and insert-time compression — real, but a small fraction of §2. | **Closed by construction, not deferred.** Added `sim/Forge.Sim.Tests/LaneTests.cs` (7 tests) with geometry chosen to expose each rule: short lanes, `speed = 1` where the off-by-one needs sub-`v` positions, and touching runs of *different* items (which cannot merge) to make advance order observable in a single tick. All five mutants now die; suite is 22/22. Also hardened `Lane.Insert` to throw on its own precondition — misuse previously created overlapping runs silently and only tripped the invariant on a LATER `Advance`, pointing at the wrong code. **Standing lesson: the `transport` golden must not be cited as evidence that belt semantics are correct — it gates the chain-level numbers only.** Residual B24 gaps (inserters, splitter golden) are unchanged by this. | resolved |
| B53 | 2026-07-17 | 2 | **§3.2 resync/baseline, the residual left open by the SignalR client work.** `SimHubClient.cs` fetched no baseline on connect/reconnect, so a client joining mid-session (or reconnecting after a drop — the hub is `Clients.All` with no replay) had no way to establish starting state, only whatever `sim.tick` happened to arrive next. | **Implemented: `GET /sim/state` on connect AND on every SignalR reconnect** (`EstablishBaselineAsync()`, wired to `HubConnectionBuilder().WithAutomaticReconnect()` + the `Reconnected` event). Exposes `HasBaseline`/`BaselineTick` so a caller can tell "baseline never happened" from "baseline is old" rather than trusting state that was never actually fetched. **Contract-vs-implementation gap found, not assumed:** §3.2 says the push rate needed for gap detection is "published in `/sim/state`" — read `adapter/SimTickService.cs`'s `Snapshot()` (read-only, did not touch it) and confirmed it publishes `tickRate` (the sim's 60Hz) and no `EmitEveryNTicks`/push-rate field at all. So the client does not trust a field the endpoint doesn't have; it learns the emit cadence empirically from the first genuine tick-to-tick delta (`SimHubClient.IsGap`, pure and unit-tested) and flags any later delta that disagrees. **Real bug found and fixed during live testing, not just at parse time:** the first live run false-flagged a gap on every connect (`baseline_tick=722`, first observed tick `894`) — the baseline fetch is an HTTP round-trip racing the independent SignalR tick stream, so by the time the baseline lands the stream is already several emits ahead; comparing the first post-baseline tick against `BaselineTick` misreads that latency as a missed message. Fixed with a one-shot `_skipNextGapCheck` flag: the tick immediately following a (re)baseline is recorded as the new reference point but not delta-checked against it; cadence learning and gap detection resume cleanly from the next tick. Verified stable across 3 consecutive live runs post-fix (`gap_detected=false` every time), vs. failing every time before it. Negative-controlled check extended: **baseline-fetch-failure control** (`EstablishBaselineAsync()` against `127.0.0.1:1`, a port nothing binds — returns `false` and `HasBaseline` stays `false`, not silently true) and **gap-detection pure control** (`IsGap` on a matching-step triple vs. a mismatched one, no network). Live leg now also asserts `HasBaseline` and `BaselineTick >= 0`, not just `Connected`. Ran the adapter ad-hoc on a dedicated port (5299), verified via `ss -tlnp` that the bound PID was mine and the binary's mtime postdated a clean `rm -rf bin obj && dotnet build`, killed by PID only — per B48's own shared-box conventions. Full existing suite re-ran green under mono, adapter both up and down. Not built (deliberately out of this unit): reconnect-after-genuine-drop hasn't been exercised against a real network interruption, only the `Reconnected` wiring is in place; wiring `SimHubClient` into `main.tscn` remains deferred, per B48. | resolved |
| B50 | 2026-07-17 | 6 | **Splitter golden + C# implementation — discharges B24 residual 2.** `transport-v0.md` §6 pinned the alternation rule and required `in_next`/`out_next` to be hashed, but nothing exercised either. Now: `Splitter` in refsim and `Forge.Sim`, a `splitter` golden scenario (miners → beltIn.lane0 → splitter → beltOut.lane0+lane1 → 8 furnaces per lane) gated in `GoldenTests`, and `SplitterTests.cs` (5 tests). Output lanes hold **[40,40]** — exactly balanced, the fairness property. All 12 pre-existing hashes (steady/backpressure/transport) byte-identical, verified by regeneration. Suite 29/29. | **Applied B49's lesson rather than repeating its mistake.** The `splitter` golden is SYMMETRIC — both outputs are drained by identical furnace groups, so neither ever blocks, and in that regime §6.2's *flip on the side actually used* and the wrong reading *flip on the side preferred* behave identically (both alternate A,B,A,B forever). The balanced [40,40] proves only that the splitter is not grossly broken. So the rule is gated by unit tests on the ASYMMETRIC case instead. Mutation-verified, and the split is exactly as predicted: *flip-on-preferred* **survives the golden** and dies in `SplitterTests`; *never-alternate* and *pointers-not-hashed* die in both. **NEW RESIDUAL — §8's evaluation order (splitters after belts) is UNOBSERVABLE in the v0 topology, therefore untested.** Swapping the order yields byte-identical hashes at every tick sampled (600→12000, transient and steady) — confirmed empirically, not assumed. This is not a gap to paper over: §8's rationale is explicitly about *multiple* belts feeding splitters, where the one-tick visibility difference becomes dependent on belt id order. With one splitter fed by one belt there is genuinely nothing to observe. Testing it needs a chained topology (splitter → belt → splitter). **§8's ordering rule is currently a claim, not a gated property** — recorded in `golden-v0.md` §3. B24 residual 1 (inserters) is untouched and remains the largest hole. | resolved |
| B54 | 2026-07-17 | 6 | **Adapter `beltDeltas` live emission — the D21 follow-on.** The adapter emitted `beltDeltas: null` ("belts not modelled") because its hosted `World` had no belts. Now that C# belts exist (B49), the adapter hosts a **transport** world by default (`Sim:Transport`, default true — the slice is defined as source→**belt**→machine→output, so a beltless world is not the slice) and emits populated `beltDeltas`. **Live-probe verified**, 30/30 assertions on a running adapter: `beltDeltas` is an array on every emit, one entry per lane, carrying `{belt, lane, spacing, length, runs[{head,len,item}]}`. Real payload: `{"belt":0,"lane":0,"spacing":16384,"length":1310720,"runs":[{"head":1306624,"len":80,"item":0}]}` — the saturated 80-item lane as a single run. **Hash parity still green** at `0x09b4e71be57a0cf9` (tick 2296). **The pinned `0xe796b3a266fa3c9c` from `e865109` is legitimately superseded**: that hash was a *beltless* world; the adapter now hosts a different (transport) world, so its state hash differs by construction, not by defect. The probe reads a new `transport` flag from `/sim/state` and reconstructs the matching world — otherwise parity would fail for a reason that is not a bug. | **Ruling on B47's ProjectReference note: it was already stale.** `adapter/adapter.csproj` has carried `<ProjectReference Include="..\sim\Forge.Sim\Forge.Sim.csproj" />` since `2819e66`; nothing needed wiring, so no decision number was spent on it. The real decision here is **D22** (beltDeltas carries absolute state, not deltas). Also exposed `Lane.Spacing` publicly: a run is a compressed block, so any consumer expanding it into item positions needs the spacing, and it is content-derived (belt tier) rather than a constant — publishing it beats every client hardcoding 16384 and breaking silently when a tier changes. Unblocks the game-side belt visualization path (B52 + SimHubClient). **TAIL ITEM (B53 finding, folded in per orchestrator):** contract §3.2 claimed the push rate was "published in `/sim/state`" — **it was not**; only `tickRate` (the 60 Hz *sim* rate) was on the wire, and the client was left to infer cadence empirically. That inference is not merely inconvenient, it is *unsound*: observing a gap of 3 cannot distinguish "cadence is 3" from "cadence is 1 and I am dropping 2 of every 3" — precisely the condition §3.2's gap detection exists to catch. **`emitEveryNTicks` is now published** (integer tick delta, default 3), chosen over a derived Hz because it is the exact integer the client compares against and a rate would go fractional the moment `tick_hz` stops dividing evenly. Additive — all prior snapshot fields intact. simprobe now **cross-checks the published value against the observed emit gap** (a field that drifts from reality is worse than no field); live-verified `published=3 == observed=3`. **Also corrected a second §3.2 defect the addendum did not flag:** its closing paragraph instructed that belts "may legitimately be deltas" because "belt contents are large... unlike three ints", and specified a tick-tagged half-open-interval delta contract. **That premise is empirically false and D22 supersedes it** — a lane is a list of *runs*, so cost is O(runs) not O(items) and a saturated 80-item lane ships as ONE run. Left as-is, a future reader would have implemented belt deltas per §3.2 and contradicted a landed decision. Struck through with the rejection reasoning kept. | resolved |
| B62 | 2026-07-17 | 6 | **Inserters v0 — B24's largest residual, and the biggest Phase 6 gate.** §3.3 calls the inserter *"the usual real bottleneck, and the one players most often mis-model"*, yet nothing implemented or exercised one: every scenario moved items machine-to-machine directly. Now specced (`transport-v0.md` §10), implemented in refsim + `Forge.Sim`, and gated by an `inserter` golden scenario + `InserterTests.cs`. **43/43 tests. All 16 prior hashes byte-identical** — verified by regeneration, and it is not luck: §10.5 adds NO count prefix because inserters really are fixed-topology in v0, which is §7.1's amended rule (*prefix iff dynamic*) applied literally rather than reflexively. | **v0 IS A STRICT SUBSET OF §3.3, and that gap is the point.** v0: one fixed `swing_ticks`, one item, `Θ_ins = tick_hz/swing_ticks`. §3.3 additionally specifies `swing_capacity` (stack size, tech-boosted) and a **position-dependent** `t_swing` — the reason chest→chest ≠ belt→machine. **Neither is implemented or gated**, so a green inserter golden does NOT discharge §3.3's formula; B24 keeps that residual. Design calls worth the review: **(1)** contention is **first-by-id, NOT round-robin** — the deliberate opposite of §6.2's splitter. A splitter exists to divide a stream so fairness is its purpose; an inserter is a dumb arm, and two arms fighting over one source is a *layout* problem. Resolving it "fairly" would hide that from the player; starving the higher-id arm is **information**. **(2)** `holding` is a separate field from `state` and is hashed — a Blocked inserter has taken from the source and not yet delivered, so the item exists nowhere else; drop the field and it is destroyed. Conservation for one bool. **(3)** §1.3's phase order (machines → inserters → belts) produces a real asymmetry, now specified rather than discovered: pickup from a lane head reads LAST tick's belt (one-tick latency) while a drop is followed by THIS tick's advance (none). Unlike §8's splitter ordering — which is unobservable and stays untested — this one is observable: the reversed-order mutant dies. **FUTURE HASH BILL, recorded so it is not rediscovered:** the moment inserters become placeable at runtime the way belts did in B56, they become dynamic and §7.1 forces a `u16` count prefix, breaking every hash exactly as D23 did. Not a reason to prefix now — that would break 16 hashes for a feature that does not exist — but the bill is known. | resolved |
| B64 | 2026-07-17 | 6 | **Inserter state on the wire.** Inserters existed in the sim (B62) but were invisible to the client. Now emitted per-inserter on `sim.tick` and `GET /sim/state`: `{id, state, holding, progress, swingTicks, item}`. Contract **§3.5**. **Additive — §3.1 is NOT re-cut**, so a pre-B64 client ignores the key and keeps working; the ideal shape is parked with the `beltDeltas`→`belts` rename for the eventual §3.1 breaking re-cut. **All 20 golden hashes byte-identical** (not 16 — B62's `inserter` scenario added four), verified by regeneration: emission is a pure read and cannot touch the sim. 43/43 tests. | Live-verified on **both branches of D21's null-vs-array rule**, which needed a new `Sim:Inserter` config to do honestly: the slice's reference build has no inserters, so the default world correctly emits `null`, and without a way to host the §10 world the array path would have shipped **never having been run**. Negative control (default world) → `null`; positive (`Sim:Inserter=true`) → 3 entries, all legs green, hash parity `0x2a7ab0426dda5f3b`. Probe cross-checks `/sim/state` against the hub — a client baselines on one (§3.2) and tracks the other, so the two disagreeing is silent corruption. Design notes: **per-inserter, not tallies** (the deliberate opposite of `machineState`) because D24 makes a starved arm *information* — `{"Idle":1,"Swinging":2}` cannot say WHICH arm is losing, erasing the signal §10.4 exists to expose. **`holding` ships despite looking derivable**: it is not — Blocked holds, Swinging holds, Idle does not, so there is no function from state to holding. | resolved |
| B67 | 2026-07-17 | 6 | **Closes both B56 v0 limitations, server side.** **(1) Chain-join** (`transport-v0.md` §2.5): a placement adjacent to an existing belt now JOINS it instead of forming an isolated stub. **(2) `rejected[]` reasons**: `occupied` and `duplicate-in-batch` join `off-map` on the wire. 49/49 tests (6 new `ChainJoinTests`), mutation-verified (4/4 die), all 20 golden hashes byte-identical. Live: a 2-cell run + a third cell → ONE 3-tile belt (`196608` Fx32), not two stubs; `{"reason":"occupied"}` and `{"reason":"duplicate-in-batch"}` observed on real responses. | **B56's deferral was reasoned from a FALSE PREMISE, and that is the finding.** It recorded that joining meant "rebuilding a live lane and either discarding its items or migrating them, and neither is specified". There is no rebuild: a head-join is `Length += n` with item positions untouched (they are measured from the tail, which did not move); a tail-join is `Length += n` plus one add per RUN (not per item), and because every run shifts EQUALLY the §1.2 spacing invariant survives by construction rather than needing re-derivation. Items are never destroyed. The J1 mutant (tail-join that skips the shift) is exactly the bug B56 feared, and it dies. **Hashes unchanged for a reason, not by luck:** §2.5 only fires via `ApplyBeltBatch` (runtime placement), and no golden scenario places belts — they build fixture belts at construction. The goldens therefore CANNOT exercise §2.5 and cannot regress from it; `ChainJoinTests` is the gate and the goldens are not (the B49 lesson). **A real bug caught pre-test:** `_beltChains` began as a List parallel to `Belts`, but scenario belts are constructed straight into `Belts` with no cells — the lists would desynchronise and a join would silently extend the WRONG belt. Now keyed by belt index; absent key = fixture belt = never a join target. Part 2 is additive: `{cell, reason}` shape unchanged, only the reason SET grew, and §3.4 already told clients not to bind to the text. The judgement runs under the tick lock through the same `World.Judge` the drain uses, so it cannot drift from the decision. **REMAINING v0 GAP — bridging:** a chain attaching on BOTH sides splices two belts into one, which is a genuine lane MERGE (concatenating item lists, deciding the seam), not an offset. Not modelled; v0 joins downstream only and leaves the upstream belt separate. Follow-up if the UI allows it. **Client consumption of the new reasons is iso's follow-up, not this slice.** | resolved |

## Decisions

Decisions that shape later work. Record the alternative rejected — it's what makes
the entry useful when someone revisits it.

| # | Date | Decision | Alternative rejected | Why |
|---|---|---|---|---|
| D21 | 2026-07-17 | **B45's tick-payload defaults: RATIFY WITH AMENDMENTS, as the client consumer.** Ratified as-is: **20 Hz push** (right cadence — `machineState` tallies and `stock` are display values needing no interpolation, and the one thing that *would* need 60 Hz, §6.1 belt chevrons at real item rate, has no producer yet; revisit with belts, not before), and **`machineState` = §4.2 aggregate tallies** (all the v0 sim *can* say — it has no spatial machines — and enough for a HUD). Two amendments, both cheap now: **(1) `stockDelta` -> `stock`, absolute levels, not deltas-since-emit.** **(2) `beltDeltas: null` in v0, not `[]`** — `null` = subsystem not modelled, `[]` = modelled, no change this tick. Plus: the resync/gap contract is now written down (`adapter-contract-v0.md` §3.2) so belts land into it rather than after it. | (1) Keeping deltas-since-emit. (2) Keeping `[]`. (3) Adding a `seq` field for gap detection. (4) Blocking on a per-entity `machineState` now. | **Amendment 1 is the load-bearing one — deltas-since-emit is unrecoverable for a client, and B45's own reasoning for it does not survive contact.** B45 justified deltas because "a snapshot would grow without bound": true of an event log, false of a *level*, which is O(1) per item type — three ints, constant forever. So the cost argument for deltas is nil, and against it: absolute levels are **strictly more informative** (the client derives a delta from two samples; it cannot derive a level from deltas without a baseline). Concretely broken as shipped: `_primed` is **server-global, not per-client**, so a client connecting at any time after the first emit gets deltas anchored to a baseline it never saw and can never reconstruct absolute stock from the stream at all; and since the hub is `Clients.All` with no replay, one dropped message means **permanent silent divergence** — the UI shows wrong stock forever with no way to detect it. Absolute levels make every emit a full resync: a gap costs one frame of staleness. This is the difference between a client that self-heals and one that needs a reconciliation protocol to be correct. (2) `[]` conflates "belts exist, nothing changed" with "belts are not implemented" — B45 flagged this itself as "the one that will actually bite". `null` distinguishes them for one line's cost, and the distinction only gets more expensive to introduce once a v1 belt consumer exists that reads `[]` as an empty-but-present network. (3) `seq` rejected as redundant: `tick` is already monotonic and emits are exactly `EmitEveryNTicks` apart, so a jump *is* the gap signal — provided the push rate is published, which `/sim/state` now must do. Fewer fields that can disagree. (4) Not blocking: per-entity state is impossible before the sim has entity identity. But recorded as a **known breaking change** — §6.1 (a stopped machine is visibly stopped) and §6.3 (per-machine alert markers, the whole reason a 0.125-zoom factory stays diagnosable) both need `{entityId: state}`, and aggregate tallies cannot express either. The client will be re-cut against that shape; better that it is written down now than discovered when the renderer needs it. **Survives belts arriving, which was the ask:** §3.2 pins that belt deltas (legitimate — belt contents are large, unlike three ints) must be tick-tagged, must cover exactly `(tick - EmitEveryNTicks, tick]` so a snapshot reconciles without double-counting, and must have full belt contents in `/sim/state` to re-baseline against. A delta stream with no anchor and nothing to rebuild from is unrecoverable — the trap `stock` was walking into. **Cost of these amendments: `StockDelta()` drops its `_lastX`/`_primed` state and returns levels (net simpler); `BeltDeltas()` returns `null`. Both are one method each in `SimTickService`, exactly as B45 predicted.** |
| D1 | 2026-07-17 | Foundation order is engine scaffold → forge integration → sim core, before any feature module. | Starting with the isometric renderer because it's the visible part. | A hardcore factory player is playing the sim; the renderer is a view onto it. State in the renderer is unfixable later. |
| D2 | 2026-07-17 | Determinism is a Phase 4 exit condition, verified by replay. | Treating it as a later hardening task. | It cannot be retrofitted onto a sim that wasn't built for it. |
| D3 | 2026-07-17 | Scope is one vertical slice (source → belt → machine → output), not breadth. | Several subsystems partially built. | 300 min buys one thing that works end to end, or five that don't. |
| D4 | 2026-07-17 | Engine is **Godot 4.7** (snap install, GDScript first, C#/GDExtension escape hatch for hot paths). Provisionally locked by the orchestrator; scaffold started in parallel. Grading confirms — see B1 for the carried gap. | Bevy (better ECS/perf fit, but needs a Rust toolchain that isn't on this box, manual async HTTP wiring, and a third-party iso-tilemap plugin); Phaser/PixiJS (weakest perf ceiling for thousands of entities); LÖVE (no axis where it beats Godot here). | Zero-friction install, built-in isometric TileMap with no plugin risk, real editor for tile iteration, GPU headroom to spare. Fallback to Bevy stays live if GDScript hits a real perf wall — trigger is a measured wall, not a suspicion. |
| D5 | 2026-07-17 | Sim core will own its own tick as engine-independent plain data; Godot's `_physics_process` drives it but does not define it. | Letting Godot's node/scene lifecycle be the sim loop. | Keeps determinism ours rather than the engine's, and keeps the Bevy fallback cheap — a sim that doesn't know about Godot can be re-hosted. Directly answers the gap in B1. |
| D6 | 2026-07-17 | **Q1 answered: an adapter/BFF layer is required.** A separate thin sim service — its own tables, EF Core bypassed on the hot path, forge MRP data read once at load/seed via REST, live state pushed over a `BoardHub`-pattern WebSocket. It owns all Postgres access; Godot never touches the DB (per B3). No language mandate on the engine — the sim needs only a client, and forge-api's .NET 9 / forge-ui's Angular are not binding. | (1) Building inside forge-api as a controller area — every mutating handler writes an `ActivityLog` row plus a capability check, which is a fine tax at ERP human rates (a few writes/sec) and disqualifying at a 20–60 Hz tick moving hundreds of items/sec. (2) Forking forge-api — loses upstream MRP logic for nothing. | Reuses forge's real MRP domain (BOM explosion, lot/serial, WorkCenter capacity) as the sim's vocabulary while keeping the enterprise audit/RBAC surface off the tick path. **Fallback if the adapter proves too costly:** the sim runs fully standalone against its own tables with forge entities imported as a static seed file — the vertical slice never depends on forge-api being up. |
| D16 | 2026-07-17 | **Adapter service skeleton delivered.** `factory/adapter/` — .NET 10 minimal API (host only has 10 SDK; task allowed 9), separate from forge-api (D6/B4), no Postgres client (D10/B12-accepted). `ForgeApiClient` (kiosk login + Part/BomRevision reads + receive-stock/use-stock writes), `ColdPathLoader` (`POST /cold-load` → real forge Part+BOM data → `factory/data/live-import.json`, sim-consumable shape mirroring `recipes-v0.toml`'s items/recipes fields), `POST /checkpoint` (delta → receive-stock/use-stock, stub-callable, no timer). Live-verified end to end, not just built: cold-load produced 2 items + 1 real recipe from live data (one BOM line added live first — see B26); checkpoint call moved real on-hand stock 10→15 through the adapter's own HTTP client, not curl. | A controller area inside forge-api (ruled out by B4/D6). Wiring an actual Postgres client for checkpoints (ruled out by D10 before this dispatch). | Closes the "adapter is a real thing, not just a contract doc" gap — the vertical slice's forge-integration seam now has running code proving D6/D10's shape holds under an actual HTTP round trip, not just design. WorkCenter mapping deliberately deferred (B25) — not needed to prove the seam. |
| D17 | 2026-07-17 | **Resolves B26, closing the D13→D14 trail.** `recipes-v0.toml` stays the **sole canonical** content source. `tools/recipes_to_json.py` is **KEPT** — repurposed as the §2.4 content validator (its validation logic plus a CI `--check` mode have standalone value independent of the dropped transform). The generated `data/recipes-v0.json` is **deleted**. **SUPERSEDED 2026-07-17 (D19) — "keep the .py" half only.** The JSON-deleted half stands. | The PM's recommendation to delete both — it arrived after the mathematician had already committed them (`a2f76bd`), and it undervalued the script. | Better than the PM's call: the *generated artifact* was the actual hazard (a second content file that looks authoritative and drifts from the TOML the moment content changes), not the script. Deleting the JSON removes the drift risk; keeping the script converts sunk work into the §2.4 validator the spec asks for and B19 will eventually need a fixture for. Trail now reads: B20 → D13 (build-step JSON) → D14 (C# parses TOML directly, transform dropped) → **D17 (script survives as validator, JSON deleted)** → **D19 (script also drops — C# `Content.Validate` owns it)**. |
| D18 | 2026-07-17 | **Tomlyn dropped from the sim loader.** Its restorable NuGet API no longer exposes the document-model entry point (verified by reflection on 2.10.1 and pinned 2.1.0), and its convention-based binding turns a misspelled content key into a silent default-0 — unacceptable in a determinism-critical loader. `factory/sim/Forge.Sim/MiniToml.cs` owns our exact content subset and throws on anything it doesn't understand. | Tomlyn (both current and pinned-old versions — reflection-verified unusable for this, not just disfavored). | D14's substance (C# parses `recipes-v0.toml` directly, no build step) holds — this only changes *which* C# code does the parsing. **Constraint `minitoml-not-general-toml`: `MiniToml.cs` must never be advertised or reused as a general TOML implementation** — it is a narrow, throw-on-unknown parser for this content subset only. |
| D19 | 2026-07-17 | **Supersedes D17's "keep the .py" half only** (the JSON-deletion half stands). `tools/recipes_to_json.py` stays deleted — the mathematician's C# sim core now includes `Content.Validate`, which owns §2.4 validation with negative tests, making the Python validator redundant. TOML remains sole canonical (D14/D17 unchanged on that point). B19's synthetic cyclic fixture rehomes to a C# negative test in `Forge.Sim.Tests` when B19 activates. | Restoring `recipes_to_json.py` per D17's original text (what the pre-redirect dispatch asked for). | D17's "keep the .py" reasoning (sunk work, §2.4 validator, CI `--check`) is now moot — `Content.Validate` does the same job in the same language as the loader it's validating, with actual negative tests, inside the project that already has to build and test anyway. Two validators (Python + C#) would be the same "second artifact that drifts" risk D17 itself warned about, just one layer up. |
| D15 | 2026-07-17 | **Sim core v0 dispatched.** Owner: factory-mechanics-mathematician. Deliverable: C# library in `factory/sim/`. Budget: 80 min. Acceptance test: the golden vector. | — | Matches plan.md's Phase 4 allocation (80 min, the run's largest single block) and its exit condition — determinism verified by replay, not assumed. Language follows D14/B7. **Sequencing risk: see B23** — this dispatch assumes Godot can host C#, which is not yet verified. |
| D14 | 2026-07-17 | **Logged verbatim per orchestrator:** "Content loader owner = C# sim core; it parses `factory/data/recipes-v0.toml` directly (Tomlyn or similar NuGet). D13's TOML→JSON build step is DROPPED unless a GDScript consumer for content data materializes later. `recipes-v0.toml` remains the single authored source." | D13's build-step JSON transform (now dropped); a GDScript-side loader. | Resolves the D13 open question in the better direction — zero build steps instead of one, and the single authored source survives intact. Follows B7's finding that the fixed-point hot path belongs on the C# side of D4's escape hatch: if the sim core is C#, the loader may as well be, and TOML's comment-carrying advantage (the whole reason it beat JSON) is preserved end-to-end with no transform to drift. **Conditional on B23** — if Godot cannot host C#, D14 and D15 both need rework. |
| D13 | 2026-07-17 | ~~**Resolves B20.**~~ **SUPERSEDED by D14** — the TOML→JSON build step is dropped; the C# sim core parses TOML directly. Retained for history; B20's Phase 4 trigger now discharges through D14 instead. Original: | TOML stays the authored source of truth for content; a **build-step transform emits JSON**, which Godot parses natively. Owner: mathematician (queued behind the golden vector). Trigger: Phase 4 content wiring — the transform must exist before the sim reads content. | Retyping content into Godot's `ConfigFile` (splits the source of truth); a third-party GDScript TOML addon (plugin risk D4 exists to avoid). | TOML earned authorship on the comments argument — the derivations in `recipes-v0.md` need to live beside the numbers, and JSON cannot hold them. A build step keeps both properties: humans author the commented TOML, the engine reads machine-friendly JSON, and neither side compromises. **Moot-trigger UPDATE — B7 has since reported and the stated trigger did not fire as framed.** The orchestrator's condition was "moot if B7 fails and D4 fires (Rust host parses TOML natively)". B7 measured GDScript at 15.2M vs C# at 280M ops/sec (~18x) and resolved toward **D4's C#/GDExtension escape hatch — not a Bevy/Rust reversal**. So there is no Rust host and `serde` never enters the picture; D13 stands. The live nuance is different and smaller: if content *loading* lands on the C# side of the hatch, C# can parse TOML via a NuGet package and the JSON transform becomes unnecessary. **Mathematician should confirm which side owns the loader before building the transform** — the answer decides whether D13 is one build step or zero. |
| D11 | 2026-07-17 | **`factory/` is its own git repo** → `github.com/armoryworks/forge-factory`, branch **`master`** (scout-created, root-free). All game work — engine, sim, docs, data — commits there. **No role commits game code to the forge repo or its siblings.** | Vendoring the game inside `forge/` as another sibling, or a subtree of forge main. | Keeps game history separate from the ERP product it borrows data from, and matches D6/D10's architectural line: the factory *consumes* forge over HTTP, it does not live inside it. Note the branch is `master`, not `main` — the standing "push straight to main" convention maps to `master` **here only**; the forge repos keep their own default. | — |
| D12 | 2026-07-17 | **Scaffold accepted (Phase 2).** Godot 4.7 via no-sudo official binary, fixed-tick `SimClock` autoload owning its own accumulator, headless `SIM_CHECK` harness, windowed Vulkan confirmed, isometric TileMap stub present. **Q2 is proven and closed** — the sim loop is ours, not the engine's. | — | This was Phase 2's exit condition (loop ticks at a stable rate and is observable) and it is met by demonstration rather than assertion, which is what B1 asked for. D4 (Godot) and D5 (sim owns its tick) both survive contact with reality. Accepted **with B16/B17/B18 outstanding** — none of them reverse the scaffold; all three are edits inside files that already exist. |
| D10 | 2026-07-17 | **Amends D6.** All factory persistence goes through forge-api over HTTP at batched checkpoint cadence. The sim owns **no** Postgres tables for the slice; the replay log (seed + input stream, D5/Phase 4's exit condition) is a flat file sim-side. D6's separate-service shape, cold-path REST reads, delta channel, and nothing-per-tick rule all stand — only the own-tables clause is dropped. | D6's original own-tables clause (adapter holds its own schema, EF Core bypassed). | The ActivityLog-per-mutation tax is only disqualifying *per tick*; at ~1/s batched checkpoints it is exactly the human-rate ERP traffic forge-api was built for, so D6's intent survives while its mechanism is no longer needed. Drops a whole schema-ownership problem: **Q5 (which source of truth do factory tables register with) is now moot and closed unasked** — the cheapest way to answer a question is to not have it. `adapter-contract-v0.md` is correct as written; no revision needed. **Reversal trigger:** checkpoint volume outgrowing forge-api's write path, which would resurrect own-tables — not expected at slice scale. |
| D9 | 2026-07-17 | **The LP "true cost"/shadow-price planner (§3.4, §2.3 raw-cost basis) is offline tooling only — not an in-game feature in v1.** It stays a design-time calibration tool and the backend for the headless LP-vs-sim conformance test (§3.4), which is where its real value is. Answers the spec's own §8 Q4. *(The orchestrator calls this "D6"; D6 here is already the adapter decision — see B6.)* | Shipping it in-game from tick 0; tech-gating it as an unlock. | §7.2's "perfect instruments and zero answers" is the sharpest line in the spec, and an in-game LP planner **is** an answer — it hands the player the optimum and the bottleneck, collapsing §7.1's round-up/round-down/re-architect trilemma that the doc calls the primary decision loop. The spec flags the tension itself and does not resolve it; this resolves it toward the axiom. Tech-gating is worse than either option: it makes the game hand-holdy exactly when the factory gets complex enough for the trilemma to matter. Revisit post-v1 as an explicitly opt-in mode, never a default. |
| D8 | 2026-07-17 | Render spec accepted as written: dimetric 2:1 at 64×32, `TILE_Z=16` carried through all transforms though v1 is single-level, layered sorting with far-corner depth keys and stable tiebreaks, no camera rotation, procedural placeholder art behind an asset-key manifest, redundant non-color encoding on every color channel. | True isometric (irrational slopes alias on belt edges — and belts are ~40% of the screen); camera rotation (4× art cost, ambiguous blueprints); per-pixel depth; borrowed asset packs. | Clears its bar on geometry, transforms, and draw order with derivations rather than assertions, and stays a pure view spec — no game state introduced. The far-corner depth key and stable tiebreak are the two classic isometric failures caught pre-emptively, which is exactly what Phase 0 is for. Colorblind redundancy is correctly marked non-negotiable given a red-green overlay. |
| D7 | 2026-07-17 | Game login reuses forge's **kiosk auth** (RFID/NFC/barcode + PIN). The sim *service* authenticates separately via a service account — a distinct concern from player login. **CORRECTED 2026-07-17 per Q4:** the original framing ("not the full JWT/RBAC surface") was **wrong**. Kiosk auth is an *alternate credential only* — it issues an ordinary JWT and every call still passes the same `[Authorize(Roles)]` + `[RequiresCapability("CAP-…")]` gates. It is a different front door to the same building, not a smaller building. B5's worry is therefore confirmed, not dispelled: the RBAC stack comes along regardless. | Full ASP.NET Identity login as the game's front door (SSO/MFA/password UX is wrong for an operator station). Note this is now the *only* live alternative — "skip RBAC via kiosk" was never on the table. | Kiosk is still the right front door (it is literally the shop-floor-operator flow, and Q7 proved the round trip live), but it buys **UX fit, not RBAC avoidance**. Consequence: capabilities must be granted to the game-operator role rather than bypassed — see B13, which is where this correction actually bites. |
| D22 | 2026-07-17 | **`beltDeltas` carries ABSOLUTE belt state, not deltas — despite its name.** Each emit reports every lane's full run list. This is **D21's `stock` ruling applied to belts**, and it follows from the contract's own text: §3.2 states resync is automatic *because* `stock` and `machineState` are absolute. True belt deltas would make belts the ONE field that cannot self-heal — a late-joining client, or one that drops a single broadcast (the hub is `Clients.All` with no replay), could never reconstruct belt contents and would diverge permanently and silently. **The field name is a known wart and is deliberately NOT being fixed now**: iso's `SimHubClient` is in flight (B52) and renaming mid-flight breaks it for zero functional gain. §3.1 already flags a forced client re-cut when `machineState` gains entity identity (`{entityId: state}`); the rename `beltDeltas` → `belts` should ride with that breaking change, not ahead of it. | (a) **True per-tick deltas** — rejected: reintroduces exactly the flaw D21 removed from `stock`, and the "a snapshot grows without bound" argument that motivated deltas is false here for the same reason it was false there. Runs are O(runs), not O(items): a saturated belt is **one run** (transport-v0.md §4), so the full state is smaller than a delta stream's bookkeeping. (b) **Rename to `belts` now** — rejected on timing, not on merit; see above. | Absolute state is strictly more informative than deltas at identical cost: a client derives a delta from two samples, but cannot derive a level from deltas without a baseline it has no way to obtain. Every emit is a full resync, so a dropped message costs one frame of staleness instead of permanent silent divergence. The consistency argument is the point — `stock` and `beltDeltas` now follow the same rule, so a client author does not have to remember which fields are which. |
| D23 | 2026-07-17 | **Belt placements are TICK-STAMPED INPUTS applied at a tick boundary, and the belt count is now hashed.** **(a)** `POST /sim/belts` **enqueues**; the tick loop drains it under the existing lock before `Step()`. Hence `202` + `{appliedAtTick,accepted,rejected[]}`, not `/checkpoint`'s `204` — the work is queued, not done, and without `appliedAtTick` a caller cannot distinguish "applied at tick N" from "silently dropped". **(b)** The batch is sorted by `(Y, X, Dir)` **inside `World.ApplyBeltBatch`**, never by arrival order: network order is wall-clock dependent, so two hosts given the same POSTs would otherwise diverge. The sort lives in the sim, where it is tested, not in the host. **(c)** **`World` stopped being a closed system.** It was a pure function of (state, tick) — which is exactly why simprobe could replay from `recipes-v0.toml` alone and match the adapter bit-for-bit. Placement makes state a function of **(content, input stream)**, so §1.3's "replays store (seed, input_stream)" now has teeth: accepted placements are recorded tick-stamped in `World.Inputs`, published on `/sim/state`, and replayed by simprobe. **Verified live both ways**: green with 0 inputs (`0xd4619b224ccd6640`), green after a 3-cell POST with 3 inputs replayed (`0xc64f65d0a46b389f`) — and it FAILED first without the stream, which is how we know the check is real. | **(a)** `204` per `/checkpoint` — rejected: that is a synchronous fire-and-forget write; this is an async apply with a tick identity. **(a')** Applying inside the HTTP handler — rejected: mutates the world mid-tick from a request thread (torn state) and makes arrival order load-bearing. **(b)** Sorting in the adapter — rejected: puts a determinism guarantee somewhere untestable. **(c)** Leaving belts exempt from a hash count prefix — rejected: transport-v0.md §7.1 justified that exemption by belts being "fixed-topology, set at construction and never change", which runtime placement falsifies. The alternative was resting on "the `len>=1` invariant probably prevents a collision" — the exact reasoning §7.1 rejects for runs. | The three hang together: an input that is not tick-stamped cannot be replayed, an input applied in arrival order cannot be reproduced, and a dynamic collection without a count prefix cannot be unambiguously hashed. Cost of (c): **all 16 published golden hashes changed.** That is the topology model changing, not a regression — and the evidence is that regenerating leaves every NON-hash field (buffers, machine states, belt contents, splitter pointers) bit-for-bit identical across all four scenarios. The simulation did not move; the ruler did. |
| D24 | 2026-07-17 | **Ratifies B62's three inserter calls (`fc5eb01`, spec `transport-v0.md` §10).** **(1)** Contention is **first-by-id, not round-robin** — starvation of the higher-id arm is *information*. **(2)** `holding` is a **separate hashed field**, not derived from `state` — conservation: a Blocked inserter's item is out of the source and not yet in the dest. **(3)** The §1.3 phase order (machines → inserters → belts) gives pickup a one-tick latency and drops none; specified in §10.3 rather than left incidental. Also ratified: **no count prefix for inserters**, per §7.1's *prefix iff dynamic*, with §10.5's future-bill note as the deferral mechanism. | **(1)** Round-robin, as §6.2's splitter does — rejected: a splitter exists to divide a stream so fairness is its purpose; an inserter is a dumb arm, and two arms fighting over one source is a *layout* problem the engine must not paper over. **(2)** Deriving `holding` from `state` — rejected: no such function exists. **(3)** Inserters after belts — rejected: it only moves the latency to the other end; what matters is that the choice is stated. **(4)** Prefixing the inserter count now — rejected: breaks 16 hashes for a feature that does not exist. | Pointer entry; the arguments live in `transport-v0.md` §10 and B62. The through-line: each call prefers **legible determinism over convenient smoothing**. The engine could hide the contention, infer the bool, or leave the order incidental — each would cost the player a fact the sim actually knows. |

## Triage — `factory-math-v0.md` §8 open questions

Taxonomy: **(a)** blocks vertical slice · **(b)** post-slice this run · **(c)** out of scope this run.

Verdict: **none of the spec's own six open questions block the slice — no (a)s.** The only
slice-blocker in that document is one it doesn't list: the missing v0 recipe set (B9).

| §8 # | Question | Class | Reasoning |
|---|---|---|---|
| 2 | Fx32 rounding: truncate vs round-half-even | **(b)** | Pin truncate before Phase 4 per §1.2, then not blocking — see B10. §1.2 already answers it; §8 re-opens it. Determinism needs *a* pinned mode, not the optimal one. The error-budget study is what's post-slice, not the pin. |
| 4 | Is the LP planner in-game, out-of-game, or tech-gated? | **(c)** | **Ruled — D9, offline calibration tool only, not in-game v1.** Out of scope as a *feature*; resolved now rather than deferred because it's an axiom question and axiom drift gets expensive late. |
| 1 | `α`, `β`, `γ` playtest fits | **(c)** | Ruled (c) by the orchestrator. Concurs: `β` is measured, not decreed, and there is nothing to measure until the game is playable — no v1 progression exists to calibrate against. |
| 3 | Fluid model at 10k segments | **(b)** | No fluids in the slice. Spec's own advice (profile before deciding) is right, and it names this the likeliest UPS cliff — so it deserves a real Phase-1 slot rather than being dropped. |
| 5 | Multiplayer: lockstep vs authoritative | **(c)** | Cost already paid: D5's determinism enables either, so deferring is free. Nothing about the slice forecloses the choice. |
| 6 | Should `σ_min` exist? | **(b)** | A balance question over one `[CAL]` constant with no architectural consequence — cheap to settle once machines actually run. |

## Open questions

Things that aren't blocking yet but will be. Promote to the blocker table the
moment they stop work.

| # | Question | Waiting on |
|---|---|---|
| Q1 | Does anything in forge-api / forge-db force a runtime or language on the factory build? | **Answered — see D6.** No language mandate; the constraint is architectural, not runtime. The real finding is the `ActivityLog`-per-mutation tax that forces the adapter layer. Soft constraint remains Phase 3's "builds the forge way" (docker-compose, sibling-repo layout) — a process convention, not a fork-lock. |
| Q2 | Does the chosen engine actually give us a fixed timestep we control, or does it own the loop? | **Answered — yes, we own it.** `game/autoload/sim_clock.gd` runs its own accumulator over `_process(delta)` (render-frame callback, not `_physics_process`) with `TICK_RATE = 20.0` as a single named constant; the renderer (`main.gd`'s `TickLabel`) only reads `SimClock.tick_count`, never writes it. Verified headless: `tools/godot4 --headless --path game` ran 3.006s real time and produced 60 ticks (expected 60.1, within tolerance) then self-quit with exit code 0. Also verified windowed with Vulkan Forward+ rendering (RADV, RX 9070 XT) — sim check still printed PASS while the window stayed open. Engine does not own the sim loop. |
| Q3 | What tick rate do the v0 recipe ratios assume? | **Answered — `TICK_HZ = 60`, explicit and calibratable**, in `factory-math-v0.md` §1 (integer tick counter, no delta-time in sim code, render interpolates via an alpha the sim cannot read). Lands at the top of the survey's 20–60 Hz bound. The cross-dependency from `isometric-design.md` §6.1 (belt chevrons animate at real item rate) **resolves cleanly** — the math report gives a per-tick craft rate the renderer can drive chevrons from directly. No flag needed. |
| Q6 | Can Godot satisfy `isometric-design.md` §7's five disqualifiers — explicit per-sprite draw order, ~20k sprites/frame with per-sprite tint, boot-time procedural atlas generation, screen-space overlays after world content, pixel-snapping at arbitrary zoom? | **Answered — all 5 PASS, empirically, via `game/scenes/engine_checks.tscn` (windowed, screenshot-sampled).** (1) draw order: z_index overrides scene-tree add order (forced red over blue where tree order alone would draw blue on top) — the hard gate clears. (2) 20k tinted `MultiMeshInstance2D` quads (per-instance color, index order = draw order) sustained 60.0 fps. (3) boot-time `Image.create()` + `fill_rect()` → `ImageTexture` + two `AtlasTexture` regions rendered correct distinct colors. (4) a `CanvasLayer` overlay held constant on-screen size (40px both at camera zoom 1 and 2) while world-space content visibly scaled (20px→40px) — confirms screen-space-after-world-space with a different transform. (5) `Viewport.snap_2d_transforms_to_pixel`/`snap_2d_vertices_to_pixel` toggle without error at a non-power-of-two zoom (0.375) with a sub-pixel camera offset; edge stayed hard-pixel in both states (Godot's flat `ColorRect` doesn't antialias by default, so this is a weaker differential signal than 1–4, but the settings exist and apply cleanly at arbitrary zoom — no disqualification). No Bevy-fallback trigger. **PM 2026-07-17 (commit `4968bb8`): the render gate is now PERMANENTLY CLOSED.** §7 framed these as "answering no to any is disqualifying" — none disqualify, so D4 (Godot) is confirmed on the render axis and no Bevy fallback can be triggered on rendering grounds again. Check (5) is noted as the weakest signal (flat `ColorRect` does not antialias by default, so the differential is soft) — accepted, since the settings exist and apply cleanly at arbitrary zoom; if diagonal shimmer ever appears in real art it reopens as a bug, not as an engine-choice question. **The perf axis stays separate and live (B7 measured, B23 pending)** — any future C#/GDExtension move is about arithmetic throughput, not rendering. **2026-07-17 follow-up (`game/scenes/iso_depth_check.tscn`):** per the render-review request for a concrete iso-grid occlusion proof (not just the abstract z_index test above), built a scene using the real §2 world→screen transform (`sx=(x-y)*32, sy=(x+y)*16`) and §3's exact far-corner formula on two footprints matching the spec's own worked example shape — a big 5×5 machine at origin (0,0) and a small 1×1 machine at (3,3) fully inside its footprint. **Reproduced the classic bug verbatim:** origin-only depth (A=0, B=6) draws the small machine on top at their shared pixel — wrong, since the big machine should occlude it. **Then fixed it**: far-corner depth (A=10, B=8) draws the big machine on top at the same pixel — correct. Both are pure `z_index` assignments, nothing engine-imposed — confirms Godot doesn't just clear the abstract hard gate, it can implement §3's actual rule as specified. `ENGINE_CHECK_Q1_FARCORNER result=PASS`. |
| Q7 | Empirical forge stack bring-up. | **Answered — verified live 2026-07-17.** Stack already up via forge-deploy's dev compose; `GET /api/v1/auth/status` → 200. Full round trip confirmed: `POST /api/v1/auth/kiosk-login` (barcode+PIN) → JWT → `GET /api/v1/parts` with Bearer token → 200 with real seeded data (2 parts). See `adapter-contract-v0.md`. Unauthenticated `GET /api/v1/parts` correctly 401s, confirming the auth gate is live, not a no-op. |
| Q4 | Is forge's kiosk auth usable standalone, or does it drag in the Identity/capability stack it's meant to avoid? | **Answered — it does NOT avoid the stack.** Kiosk login is only an alternate *credential-entry* mechanism (barcode+PIN instead of email+password) — it still runs through `UserManager<ApplicationUser>` (full ASP.NET Identity) and issues a normal JWT with role claims. Every subsequent request still hits the same `[Authorize(Roles=...)]` + `[RequiresCapability("CAP-...")]` gates as password login (confirmed: the test kiosk user needed an assigned role AND `CAP-MD-PARTS` enabled to read Parts). D7's "not the full JWT/RBAC surface" framing is inaccurate — kiosk auth changes *how you log in*, not *what you're authorized to do afterward*. Resolves B5's caveat: not blocking, but D7 should be corrected, not just accepted-with-caveat. |
| Q5 | ~~Which schema source of truth do the factory's tables register with?~~ **MOOT — closed 2026-07-17 by D10.** The slice ships no factory tables, so the question has no subject. Resurrects only if D10's reversal trigger fires (checkpoint volume outgrowing forge-api's write path). | ~~Phase 3. Survey says own tables can sit in the shared Postgres without fighting either; the choice is only forced if we want them tracked. |
